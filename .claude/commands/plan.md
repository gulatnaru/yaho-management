---
description: 확정된 요구사항을 설계와 구현 순서로 옮긴다
argument-hint: [기능 이름 또는 이슈 번호]
---

architect 서브에이전트를 사용해 다음 기능을 설계해줘.

대상: $ARGUMENTS

prisma/schema.prisma 와 docs/DATABASE.md 를 읽고 현재 모델과 맞출 것.
DB 제약으로 막을 것과 서버 규칙으로 막을 것을 명시적으로 구분할 것.
구현 순서는 각 단계가 독립적으로 검증 가능하도록 쪼갤 것.

설계가 나오면 내 승인을 받고 멈출 것.
