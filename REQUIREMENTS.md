# YAHO Management System Requirements v2

## 1. 제품 개요

### 1.1 목적
YAHO 운영자가 아이, 친구/형제관계, 프로그램, 클래스 일정, 예약, 결제/환불, 매출을 하나의 웹앱에서 관리할 수 있도록 한다.

### 1.2 사용자
- 운영 관리자
- 선생님
- 향후 역할 확장이 가능하도록 권한 구조를 설계한다.

### 1.3 핵심 원칙
- 고객 1명 = 아이 1명으로 관리한다.
- 아이 간 관계를 별도 데이터로 관리한다.
- 친구관계와 형제/자매관계를 구분한다.
- 클래스 기본 정원은 8명이며 관리자가 클래스별로 1~99명 범위에서 조정할 수 있다. 정원이 찬 경우에도 관리자는 명시적으로 확인한 뒤 초과 예약할 수 있다.
- 기본 클래스에는 선생님 2명을 배정한다.
- 클래스 전체 취소와 아이 개인 예약 취소를 구분한다.
- 환불은 아이별 예약 기준으로 관리한다.
- 매출은 결제/할인/환불을 반영하여 집계한다.

---

# 2. 도메인 구조

```text
아이
 ├── 친구관계 ─── 아이
 ├── 형제/자매관계 ─── 아이
 │
 └── 예약
      └── 클래스
           ├── 프로그램
           ├── 선생님 2명
           └── 클래스 취소

예약
 └── 결제
      └── 환불
```

## 2.1 핵심 Entity

- User
- Child
- Relationship
- Teacher
- Program
- ClassSchedule
- Reservation
- Payment
- Refund

---

# 3. 아이 관리

## 3.1 기본 규칙
- 시스템의 기본 고객 단위는 아이 1명이다.
- 보호자 정보는 해당 아이 정보에 포함하거나 별도 확장 가능한 구조로 설계한다.
- 아이 삭제는 이력 보존을 고려하여 기본적으로 비활성화 방식으로 처리한다.

## 3.2 아이 정보
필수/선택 여부는 실제 화면 설계 단계에서 확정한다.

- 아이 이름
- 생년월일
- 성별
- 보호자 이름
- 보호자 연락처
- 메모
- 등록일
- 활성/비활성 상태

안전 정보 및 동의 항목은 20-1장에서 별도로 정의한다.

## 3.3 아이 목록
목록은 식별에 필요한 최소 항목만 기본으로 보여주고, 나머지는 아이 상세(3.4)에서 확인한다.
이 원칙은 화면 크기(모바일/데스크톱)와 무관하게 적용되는 일반 원칙이다. 자세한 배경과 다른 목록 화면으로의 확장은 21-1장을 따른다.

아이 목록의 기본 노출 항목은 다음과 같다.

- 이름
- 보호자 연락처 (입력되어 있으면 tel: 링크로 표시한다)
- 다음 예약 (가장 가까운 미래 RESERVED 예약 1건만 표시하고, 없으면 –로 표시한다. Phase 5 완료 전까지는 컬럼 자리만 두고 값은 비워둔다 — 21-1.3, 22장 Phase 5 참고)

다음 항목은 목록 기본 노출에서 제외하고 아이 상세(3.4)에서 확인한다.

- 생년월일/연령
- 친구 수
- 형제/자매 수
- 누적 예약 횟수
- 상태

## 3.4 아이 상세
아이 상세 화면에서 다음 정보를 한 번에 확인한다.

- 기본 정보
- 친구관계
- 형제/자매관계
- 예약·출결·결제 상태 통합 이력
- 운영 메모
- 안전 정보 및 동의 현황

## 3.5 아이 통합 이력
아이 상세는 기존 기본 정보·보호자 연락처·안전/주의정보·동의 현황·운영 메모·수정/운영 액션을 유지하면서, 해당 아이의 예약·클래스·출결·결제 상태를 아이 단위의 통합 운영 이력으로 제공한다. 보호자 연락처를 별도 요약 카드로 중복 강조하지 않는다.

### 상단 요약
- 총 예약: 해당 Child에 연결된 모든 Reservation 행의 수다. RESERVED, COMPLETED, NO_SHOW, CANCELLED를 모두 포함한다.
- 참석: `attendance=PRESENT`인 예약 수다. 이후 Reservation.status가 CANCELLED로 바뀌어도 보존된 attendance가 PRESENT이면 포함한다.
- 불참: `attendance=ABSENT`인 예약 수다. 이후 Reservation.status가 CANCELLED로 바뀌어도 보존된 attendance가 ABSENT이면 포함한다.
- 취소: 현재 `Reservation.status=CANCELLED`인 예약 수다. attendance 값과 무관하게 센다.
- 예정: `Reservation.status=RESERVED`이고 `ClassSchedule.status=SCHEDULED`이며 `ClassSchedule.endsAt>=now`인 예약 수다. 과거인데 RESERVED로 남은 예약은 예정에서 제외한다(8.5, 10.3, ADR-026/ADR-028 참고).
- 이 지표들은 서로 다른 원본 상태축을 집계하므로 상호 배타적이지 않으며, 총 예약이 참석+불참+취소+예정과 반드시 같을 필요는 없다.

### 예정된 클래스와 지난 이력
- 예정된 클래스에는 위 "예정" 조건을 만족하는 정상 예약만 표시하고, 가까운 클래스부터 확인할 수 있어야 한다.
- 지난 이력에는 COMPLETED, NO_SHOW, 표시상 종료된 RESERVED, Reservation.status=CANCELLED인 예약, 그리고 ClassSchedule.status=CANCELLED이면서 Reservation.status=RESERVED인 예약을 표시한다. Reservation이 CANCELLED이거나 ClassSchedule이 CANCELLED이면 클래스 날짜가 미래여도 예정 영역에 두지 않고 즉시 지난 이력에 포함한다.
- 출결 후 CANCELLED된 예약은 취소 상태와 보존된 attendance를 함께 표시한다. CANCELLED+PRESENT는 "취소됨 · 참석", CANCELLED+ABSENT는 "취소됨 · 불참", CANCELLED+attendance null은 "취소 · 출결 미처리" 의미로 확인할 수 있어야 한다.
- ClassSchedule.status=CANCELLED, Reservation.status=RESERVED인 이력은 "수업 취소"와 원본 예약 상태를 구분해 표시한다. 수업 취소를 예약 취소로 간주하거나 Reservation.status를 변경하지 않으며 별도 "취소된 수업" 영역은 만들지 않는다(docs/DECISIONS.md ADR-046 참고).
- ClassSchedule.status=CANCELLED, Reservation.status=RESERVED인 이력에는 "예약 취소" 액션을 노출하지 않는다. 예약 상세와 예약 취소 직접 URL도 같은 취소 가능 조건을 사용하며, 직접 URL에서는 취소 가능한 form을 제공하지 않는다.
- 예정 예약은 가까운 클래스부터, 지난 이력은 최근 클래스부터 정렬한다. 동일 클래스 시각의 결정적 tie-breaker는 기존 데이터로 구현 가능한 기술 세부사항이므로 PLAN에서 정한다.

### 이력별 표시와 이동
- 각 항목에서 클래스 날짜/시간, 프로그램/클래스, Reservation 상태, attendance 상태, Payment 상태를 서로 독립적으로 확인한다.
- Payment.status=PAID는 "결제완료", PARTIAL_REFUNDED는 "부분환불", REFUNDED는 "전액환불", CANCELLED 또는 Payment가 없으면 "미결제"로 표시한다. 이 매핑은 아이 통합 이력 전용 상세 표시이며 Phase 12 참가자 화면의 빠른 확인용 이진 요약(ADR-044)을 변경하지 않는다.
- Reservation 취소 상태로 Payment를 추론하거나, Payment 상태로 attendance를 추론하거나, 조회 과정에서 원본 상태를 변경하지 않는다.
- 아이 통합 이력에는 결제금액, 환불금액, 환불 가능 금액을 표시하지 않는다. 기존 Payment/Refund 정책과 상세 관리 화면은 그대로 유지한다.
- 각 항목에서 기존 클래스 상세와 기존 예약 상세(`/reservations/[id]`)로 이동할 수 있어야 한다.
- 새 예약은 기존 `/reservations/new?childId=<id>` 진입, ReservationForm, Zod, Server Action 및 예약 생성 검증을 그대로 재사용한다.

### 노출 수·모바일·조회 원칙
- 지난 이력은 최신 10건만 먼저 표시하고, "더보기"를 통해 중복·누락 없이 이전 이력을 계속 확인할 수 있어야 한다. 추가 조회의 기술 방식과 한 번에 가져올 양은 PLAN에서 기존 구조에 맞춰 정한다.
- 모바일에서 상단 5개 지표와 예정/지난 이력, 예약·출결·결제·취소 상태 및 이동 액션을 가로 스크롤 없이 확인할 수 있어야 한다. 대형 데스크톱 표를 모바일에 강제하지 않는다.
- 아이 상세의 관리자 접근 및 기존 안전정보 경계를 유지한다. 통합 이력 조회는 safetyInfo, Refund 상세·금액, 불필요한 보호자 개인정보를 가져오지 않으며 일반 아이/예약/클래스/결제 목록에 민감정보를 새로 노출하지 않는다.
- Reservation→ClassSchedule→Program→PaymentItem→Payment 관계를 아이 단위로 조회할 때 N+1을 만들지 않는다. 상단 요약은 정확해야 하고, 지난 이력의 초기 조회는 10건으로 제한한다.
- 기존 Child, Reservation, ClassSchedule, attendance, PaymentItem, Payment 필드만 사용한다. 신규 model/field/enum, migration, backfill은 요구하지 않는다(docs/DECISIONS.md ADR-045 참고).

---

# 4. 친구관계 관리

## 4.1 목적
함께 수업에 참여하는 아이들의 관계를 관리한다.

## 4.2 규칙
- 아이와 아이 사이의 관계로 관리한다.
- 친구관계는 양방향 관계로 취급한다.
- 동일한 친구관계를 중복 등록할 수 없다.
- 자기 자신을 친구로 등록할 수 없다.
- 친구관계 삭제/해제가 가능해야 한다.

## 4.3 활용
예약 시 특정 아이의 친구를 빠르게 검색할 수 있도록 한다.

예:
```text
김하윤
친구: 이서준, 박지우
```

---

# 5. 형제/자매 관계 관리

## 5.1 규칙
- 아이와 아이 사이의 관계로 관리한다.
- 형제/자매 관계는 양방향으로 조회한다.
- 동일 관계 중복 등록 금지
- 자기 자신 등록 금지
- 관계 해제 가능

## 5.2 관계 타입
초기에는 다음 타입을 지원한다.

- SIBLING

향후 필요 시:
- BROTHER
- SISTER
등으로 확장할 수 있도록 설계한다.

---

# 6. 선생님 관리

## 6.1 기본 규칙
- 클래스에는 선생님 2명을 배정한다.
- 선생님은 별도 Entity로 관리한다.
- 동일 클래스에 같은 선생님을 중복 배정할 수 없다.
- 향후 클래스별 선생님 수가 변경될 가능성을 고려하여 DB는 고정 컬럼 2개보다 관계 테이블 방식으로 설계한다.

## 6.2 선생님 정보
- 이름
- 연락처
- 활성/비활성
- 메모

---

# 7. 프로그램 관리

## 7.1 프로그램과 클래스의 차이

프로그램:
> 어떤 수업인가?

클래스:
> 언제, 어디서, 누가 운영하는가?

예:
```text
프로그램
"가을 숲 탐험"

클래스
2026-09-05 10:00
교하중앙공원
선생님 2명
정원 8명
```

## 7.2 프로그램 정보
- 프로그램명
- 설명
- 대상 연령
- 기본 소요시간
- 기본 가격
- 운영 상태
- 프로그램 메모

## 7.3 프로그램 상태
- ACTIVE
- INACTIVE

비활성 프로그램은 신규 클래스 생성 시 선택하지 못하도록 한다.

---

# 8. 클래스 일정 관리

## 8.1 정의
클래스는 실제 운영되는 하나의 수업 단위다.

## 8.2 기본 정보
- 프로그램
- 날짜
- 시작시간
- 종료시간
- 장소
- 정원
- 선생님
- 상태
- 메모

## 8.3 정원
- `ClassSchedule.capacity`는 해당 클래스의 운영 기준 정원을 뜻하며 기본값은 8명이다.
- 정원은 관리자가 클래스별로 1~99명의 정수 범위에서 변경할 수 있다. 99명은 운영 권장 인원이 아니라 비정상 입력을 막는 application validation 상한이다. 현재 DB는 `capacity >= 1`만 보장하고 이미 모든 클래스에 값이 있으므로 새 필드, DB 상한 CHECK, 기존 데이터 backfill은 필요하지 않다.
- 정원이 찬 경우에도 관리자는 예약을 초과 생성할 수 있다. 대기예약으로 전환하지 않으며, 관리자 초과 예약은 기존 예약과 같은 `Reservation`으로 저장한다(docs/DECISIONS.md ADR-043 참고).
- 초과 예약을 허용하더라도 동일 아이 중복 예약, 비활성 아이 차단, 취소/완료 클래스 차단 등 기존 예약 생성 규칙은 그대로 적용한다.
- 진행 예정 클래스의 좌석 점유 인원은 `Reservation.status=RESERVED`인 예약만 센다. CANCELLED는 좌석을 점유하지 않으며, COMPLETED/NO_SHOW는 진행 예정 클래스의 잔여석 계산 대상이 아니다.
- 진행 예정 클래스의 잔여석은 `capacity - RESERVED 예약 수`로 계산한다. 결과가 양수이면 "잔여 N석", 0이면 "만석", 음수이면 절댓값을 사용해 "정원 초과 N명"으로 표시한다. 종료된 클래스에는 잔여석·만석·초과 문구 대신 "종료"를 표시한다.
- 정원 미만이면 기존 예약 검증 통과 후 바로 예약한다. 정원 이상이면 초과 예약임을 경고하고 관리자가 명시적으로 확인한 경우에만 허용한다.
- 초과 예약 확인 전후로 인원이 달라질 수 있으므로, 예약 생성 시 클래스 행 잠금과 최신 RESERVED 예약 수 재확인(ADR-023)을 유지하고 서버가 최신 인원 기준으로 초과 여부를 다시 판정해야 한다(docs/DECISIONS.md ADR-044 참고).

## 8.4 선생님
- 클래스에는 원칙적으로 선생님 2명을 배정한다.
- 다만 최소 1명이 배정되면 클래스를 생성/저장할 수 있다(상한 없음, 서버가 강제로 막지 않는다). 2명 미만으로 배정된 클래스는 상세 화면에 경고 배너를 표시한다(차단이 아닌 경고 — docs/DECISIONS.md ADR-019 참고).
- 클래스 생성/수정 시 선생님을 지정할 수 있다.
- 동일 선생님 중복 배정 금지

## 8.5 클래스 상태
- SCHEDULED: 예정
- COMPLETED: 진행완료
- CANCELLED: 취소

COMPLETED 또는 CANCELLED 상태의 클래스는 기본정보(프로그램/일정/장소/정원/선생님 등)를 수정할 수 없다(docs/DECISIONS.md ADR-020 참고).

Phase 5 시점 기준으로, COMPLETED는 실제로 DB에 기록되는 값이 아니다. 클래스를 COMPLETED로 전환하는 쓰기 경로는 아직 만들지 않았고, 대신 `endsAt`이 현재 시각보다 과거이면 목록/상세 화면에서 "완료"로 표시만 한다(DB의 status 값은 SCHEDULED로 그대로 남는다). 지난(완료로 표시되는) 클래스의 목록/상세 화면에는 "예약 추가" 버튼을 노출하지 않는다. 실제 DB상 COMPLETED 전환(배치/수동 확정 등)을 도입할지는 24장 백로그를 따른다(docs/DECISIONS.md ADR-026 참고).

완료로 표시되는 클래스는 CANCELLED/COMPLETED와 동일하게 기본정보 수정이 불가하며, 클래스 취소도 할 수 없다(서버가 차단, 버튼도 숨김). 이미 끝난 클래스에 대한 수정/취소는 성립하지 않는다는 원칙이다(docs/DECISIONS.md ADR-027 참고).

## 8.6 클래스 목록 기본 노출
클래스 목록의 기본 상태 필터는 SCHEDULED(예정)이며, 정렬은 시작일시(startsAt) 오름차순으로 다가오는 클래스가 위에 오도록 한다. 지난/취소/완료된 클래스는 상태 필터에서 명시적으로 선택해야 조회된다(숨기는 것이 아니라 기본값에서 걸러둘 뿐 항상 조회 가능하다). 이는 "목록 기본 필터는 지금 처리해야 할 것만 보여준다"는 일반 원칙의 적용이며, 같은 원칙을 Phase 5 예약 목록 설계에도 적용한다(docs/DECISIONS.md ADR-022 참고). 이 "예정" 필터는 이제 8.5의 "완료" 화면 표시와 같은 기준(endsAt 기준 조회 시점 계산)으로 동작한다 — `status === SCHEDULED AND endsAt >= now`인 클래스만 조회하고, 지난 클래스는 "완료" 필터를 명시적으로 선택해야 조회된다(`status === SCHEDULED AND endsAt < now`). "취소"/"전체" 필터는 시간 조건 없이 기존 그대로 동작한다(docs/DECISIONS.md ADR-028 참고 — ADR-026의 화면 표시 판정을 목록 필터 조회에도 확장 적용한 것이며, 이전에 "필터는 DB status 기준이며 화면 표시와는 별개"라고 적었던 문장을 대체한다).

## 8.7 클래스 반복 등록
- 기존 `/classes/new` 클래스 생성 화면에서 단건 등록과 반복 등록 모드를 선택할 수 있어야 하며, 기존 단건 등록 workflow와 동작은 유지한다.
- 반복 등록은 같은 달에 속하는 시작일과 종료일, 하나 이상의 요일을 선택하는 방식으로 제공한다. 시작일과 종료일을 모두 포함하며, 선택한 기간 안에서 선택한 요일에 해당하는 날짜마다 클래스를 생성한다. 토요일과 일요일처럼 복수 요일을 함께 선택할 수 있다.
- 특정 날짜를 직접 여러 개 선택하거나, 서로 다른 달에 걸친 범위를 한 번에 등록하는 기능은 제공하지 않는다.
- 반복 등록에서는 날짜만 달라지고 프로그램, 시작·종료 시간, 장소, 정원, 선생님, 메모, 보험 여부, 보험사, 증권번호, 안전 메모는 모든 생성 클래스에 동일하게 적용한다. 날짜별 개별 수정은 제공하지 않는다.
- 기존 "직전 클래스 보험정보 불러오기"를 반복 등록에서도 사용할 수 있으며, 운영자가 최종 확정한 보험 입력값을 모든 생성 클래스에 동일하게 적용한다(ADR-032).
- 저장 전에 반복 조건, 생성될 날짜 목록, 총 생성 클래스 수를 운영자가 확인할 수 있는 미리보기를 반드시 제공한다.
- 반복 생성 대상 날짜 중 현재 KST 기준 과거 날짜가 하나라도 있으면 전체 요청을 거부한다. 과거 날짜만 제외해 나머지를 생성하지 않는다. 기존 단건 등록의 과거 날짜 허용 동작은 변경하지 않는다.
- 기존 ClassSchedule과 프로그램, `startsAt`, `endsAt`, 장소가 모두 같으면 중복 클래스로 판단한다. 대상 중 하나라도 중복이면 전체 요청을 거부하고 나머지 날짜도 생성하지 않는다. 선생님의 다른 클래스와 시간이 겹치는지는 Phase 15에서 새로 차단하지 않는다.
- 대상 중 하나라도 입력 검증, 활성 프로그램·선생님 검증, 중복, 과거 날짜 또는 기존 클래스 생성 규칙을 위반하면 어떤 클래스도 생성하지 않는 all-or-nothing 결과를 보장한다. 가능한 날짜만 부분 생성하지 않는다.
- 반복 등록으로 생성된 각 클래스는 서로 독립적인 `ClassSchedule`이다. 별도 recurrence/series/group 관계를 저장하지 않으며 생성 후 수정·취소·예약·출결·결제·환불은 기존 방식대로 클래스별로 처리한다(docs/DECISIONS.md ADR-047 참고).

---

# 9. 클래스 취소

## 9.1 클래스 전체 취소
클래스 자체가 취소되는 경우와 아이 개인 예약 취소를 반드시 구분한다.

이미 완료로 표시되는 클래스(8.5 참고)는 취소할 수 없다(docs/DECISIONS.md ADR-027 참고).

예:
```text
2026-09-05 가을 숲 탐험
→ 클래스 전체 취소
```

## 9.2 취소 정보
- 취소 여부
- 취소일시
- 취소사유 코드
- 상세 취소사유
- 취소 처리자

## 9.3 취소 사유 코드
초기 권장:

- WEATHER: 날씨
- SAFETY: 안전 문제
- MINIMUM_ENROLLMENT: 최소 인원 미달
- OPERATION: 운영 사정
- OTHER: 기타

## 9.4 클래스 취소 처리
클래스 취소 시 기존 예약을 삭제하지 않는다.

예약 이력은 유지하고 클래스가 취소되었다는 사실을 참조할 수 있어야 한다.

필요한 경우 해당 예약의 환불 상태를 별도로 처리한다.

---

# 10. 예약 관리

## 10.1 예약 단위
예약은 아이 1명 단위로 관리한다.

예:
```text
클래스 A
├── 김하윤 예약
├── 이서준 예약
└── 박지우 예약
```

따라서 친구 3명이 함께 신청해도 각각 독립적인 예약/결제/환불 이력을 가질 수 있어야 한다.

## 10.2 예약 정보
- 아이
- 클래스
- 예약일시
- 예약 상태
- 참가 상태
- 결제 정보
- 메모

## 10.3 예약 상태
- RESERVED: 예약
- CANCELLED: 취소
- COMPLETED: 참여완료
- NO_SHOW: 노쇼

Phase 5 시점 기준으로, COMPLETED와 NO_SHOW는 실제로 DB에 기록되는 값이 아니다(출결을 다루는 Phase 6 이전이므로 실제 참석 여부를 알 수 없다). 대신 RESERVED 상태인 예약 중 소속 클래스가 8.5 기준으로 "완료"(endsAt이 현재 시각보다 과거)이면, 예약 목록/상세에서 "예약됨" 대신 "완료"로 표시만 한다(DB의 status 값은 RESERVED로 그대로 남는다). CANCELLED 예약은 이 규칙과 무관하게 그대로 "취소"로 표시한다. 실제 참석 여부에 따른 진짜 COMPLETED/NO_SHOW 전환은 Phase 6(안전 정보/출결)에서 다룬다(docs/DECISIONS.md ADR-026 참고).

예약 목록의 상태 필터도 이 화면 표시와 같은 기준으로 동작한다. "예약됨"(RESERVED) 필터는 `status === RESERVED AND 소속 클래스.endsAt >= now`를 조회하고, "완료"(COMPLETED) 필터는 `status === RESERVED AND 소속 클래스.endsAt < now`를 조회한다. "취소"(CANCELLED)/"노쇼"(NO_SHOW)/"전체" 필터는 시간 조건 없이 기존 그대로 동작한다(docs/DECISIONS.md ADR-028 참고).

## 10.4 예약 생성 규칙
- 비활성 아이는 신규 예약 대상에서 제외할 수 있다.
- 취소된 클래스에는 신규 예약할 수 없다.
- 완료로 표시되는(표시상 ENDED, 8.5 참고) 클래스에도 신규 예약할 수 없다. 서버는 예약 생성 시 클래스 잠금 조회에서 `endsAt`도 함께 확인해 이를 차단한다(docs/DECISIONS.md ADR-029 참고).
- 정원 미만이면 기존 예약 검증을 통과한 뒤 바로 예약할 수 있다. 정원 이상이면 현재 인원과 정원을 알리는 초과 예약 경고를 보여주고, 관리자가 명시적으로 확인한 경우에만 예약할 수 있다. 서버는 클래스 행 잠금 후 최신 RESERVED 예약 수로 초과 여부를 다시 판정한다. 초과 예약은 대기 상태나 별도 예약 유형을 만들지 않는다(docs/DECISIONS.md ADR-043, ADR-044 참고).
- 동일 아이가 동일 클래스에 중복 예약할 수 없다.

## 10.5 친구와 함께 예약
예약 화면에서 아이를 선택하면 등록된 친구/형제자매를 확인하고 함께 예약할 수 있도록 한다.

단, 실제 예약은 아이별 별도 Reservation으로 생성한다.

---

# 11. 아이 개인 예약 취소

## 11.1 클래스 취소와 구분
클래스가 정상 진행되어도 특정 아이만 예약을 취소할 수 있다.

소속 클래스가 완료로 표시되는 예약(10.3 참고)은 취소할 수 없다(docs/DECISIONS.md ADR-027 참고).

`Reservation.status=RESERVED`인 예약은 소속 클래스가 `status=SCHEDULED`이고 취소 시점에 `endsAt>=now`일 때만 취소할 수 있다. ClassSchedule이 CANCELLED이면 수업 취소 이력과 Reservation의 원본 RESERVED 상태를 유지하고, 아이 이력·클래스 참가자·예약 상세에서 예약 취소 액션을 노출하지 않으며 `/reservations/[id]/cancel` 직접 접근에서도 취소 가능한 form을 제공하지 않는다. 서버의 최종 조건부 쓰기 검증은 동일 조건을 독립적으로 재확인해 직접 제출을 fail-close해야 한다.

이 조건은 출결 후 COMPLETED/NO_SHOW 예약의 사후 취소를 허용하고 attendance 이력을 보존하는 ADR-033을 변경하지 않는다. 이미 CANCELLED인 예약과 출결 없이 종료된 클래스의 RESERVED 예약을 차단하는 기존 규칙도 유지한다.

예:
```text
클래스 정원 8명

A 참여
B 참여
C 취소
D 참여
```

## 11.2 개인 취소 정보
- 취소일시
- 취소사유 코드
- 상세 사유
- 처리자

## 11.3 개인 취소 사유
초기 권장:

- PERSONAL: 개인 사정
- ILLNESS: 질병/컨디션
- SCHEDULE: 일정 변경
- WEATHER: 날씨
- DUPLICATE: 중복 예약
- OPERATION: 운영 사정
- OTHER: 기타

---

# 12. 결제 관리

## 12.1 결제 단위
결제는 아이별 예약을 기준으로 관리한다.

## 12.2 결제 정보
- 예약
- 결제금액
- 할인금액
- 실제 결제금액
- 결제일시
- 결제수단
- 결제상태

Phase 8에서는 관리자가 할인금액을 자유 입력한다. `discountAmount`는 0 이상이고 정가(`amount`)를 초과할 수 없으며, 실제 결제금액(`paidAmount`)은 `amount - discountAmount`로 계산한다. 별도 쿠폰/프로모션/할인 정책 엔진은 Phase 8 범위에 포함하지 않는다(docs/DECISIONS.md ADR-038).

## 12.3 결제 상태
- PAID
- PARTIAL_REFUNDED
- REFUNDED
- CANCELLED

## 12.4 결제수단
초기에는 확장 가능한 구조로 설계한다.

예:
- CARD
- TRANSFER
- CASH
- OTHER

---

# 13. 환불 관리

## 13.1 핵심 규칙
환불은 클래스 단위가 아니라 아이의 예약/결제 단위로 관리한다.

결제가 존재하고 남은 환불 가능 금액이 있으면 예약 상태와 무관하게 관리자가 수동 환불할 수 있다. RESERVED / COMPLETED / NO_SHOW / CANCELLED를 모두 허용하며, 클래스 전체 취소 건도 같은 흐름을 사용한다. 예약 취소 여부와 환불 가능 여부는 서로 독립적이고 자동 환불은 하지 않는다(docs/DECISIONS.md ADR-035/ADR-039).

## 13.2 환불 정보
- 예약
- 결제
- 환불금액
- 환불일시
- 환불사유 코드
- 상세 환불사유
- 처리자
- 환불 상태

## 13.3 환불 사유
초기 권장:

- PERSONAL
- ILLNESS
- WEATHER
- SCHEDULE
- DUPLICATE_PAYMENT
- CLASS_CANCELLED
- OPERATION
- OTHER

NO_SHOW 예약의 예외 환불은 상세 환불사유를 필수로 기록한다. 그 외 예약 상태의 환불은 상세 사유를 필수로 강제하지 않는다(docs/DECISIONS.md ADR-034/ADR-039).

## 13.4 부분 환불
부분 환불을 고려하여 결제금액과 환불금액을 별도로 관리한다.

예:
```text
결제금액 40,000원
환불금액 20,000원
최종 매출 반영액 20,000원
```

## 13.5 클래스 전체 취소와 환불
클래스가 취소되더라도 환불 기록을 자동 삭제하지 않는다.

환불 처리 여부를 확인할 수 있어야 한다.

---

# 14. 매출 관리

## 14.1 목적
운영자가 일/주/월 단위 매출을 확인할 수 있도록 한다.

## 14.2 기본 지표
- 총 예약 건수
- 참여 아동 수
- 총 정가
- 총 할인금액
- 총 결제금액(실결제액)
- 총 환불금액
- 순매출

## 14.3 기본 계산 개념
```text
총 정가(amount)
- 총 할인금액(discountAmount)
= 총 결제금액(paidAmount)

총 결제금액(paidAmount)
- 총 환불금액
= 순매출
```

총 정가는 `PaymentItem.amount`, 총 할인금액은 `PaymentItem.discountAmount`, 총 결제금액은 `PaymentItem.paidAmount`의 합계다. 순매출은 해당 기간의 실결제액 합계에서 해당 기간에 실제 처리된 환불금액 합계를 뺀 값이다. 정가-할인=실결제액의 관계가 화면에서 명확히 보여야 한다(docs/DECISIONS.md ADR-040).

## 14.4 기간별 조회
- 오늘
- 이번 주
- 이번 달
- 기간 직접 선택

기간 판정은 KST를 사용한다. 이번 주는 월요일 시작·일요일 종료이며, 시작일과 종료일을 모두 포함한다. 기간 직접 선택도 양 끝 날짜를 모두 포함한다. 매출 화면 최초 진입 시 기본 기간은 이번 달이고, 관리자가 기간을 변경할 수 있어야 한다(docs/DECISIONS.md ADR-040).

같은 조회 기간이라도 지표별 귀속 기준은 다음처럼 서로 다르며, 억지로 하나의 시간축으로 맞추지 않는다.

- 예약 수와 참여 인원: 클래스일(`ClassSchedule.startsAt`) 기준
- 정가·할인금액·실결제액: 결제 처리일(`Payment.paidAt`) 기준
- 환불금액: 환불 처리일(`Refund.refundedAt`) 기준

늦게 처리된 환불은 과거 기간을 소급 수정하지 않고 실제 환불 처리일이 속한 기간에 반영한다. 예를 들어 7월 결제를 8월에 환불하면 7월 결제와 8월 환불로 각각 집계하며, 8월 순매출은 8월 실결제액에서 8월 환불액을 뺀 값이다(docs/DECISIONS.md ADR-040).

## 14.5 프로그램별 집계
프로그램별로 다음을 확인할 수 있어야 한다.

- 예약 수
- 참여 아동 수
- 결제금액
- 환불금액
- 순매출

프로그램별 금액은 `PaymentItem`이 연결된 예약의 클래스 프로그램에 귀속한다. 기간 필터는 14.4의 지표별 시간축을 각각 적용한다.

## 14.6 클래스별 집계
클래스별:
- 정원
- 예약 인원
- 취소 인원
- 참여 인원
- 매출
- 환불
- 순매출

클래스별 금액은 `PaymentItem`이 연결된 예약의 클래스에 귀속한다. 기간 필터는 14.4의 지표별 시간축을 각각 적용한다.

## 14.7 Phase 9 착수 전 확인
매출 집계는 `Payment`가 아니라 예약별 금액 명세인 `PaymentItem`을 기준으로 작성한다(docs/DECISIONS.md ADR-005). 정가(`amount`), 할인금액(`discountAmount`), 실결제액(`paidAmount`), 환불누계(`refundedAmount`)는 Phase 8에서 이미 분리해 보관하고 있으며, 환불 상한과 누계 일관성은 ADR-007을 따른다.

예약 수에는 수업 전에 취소되어 `status=CANCELLED`이고 출결 이력이 없는 예약을 제외한다. COMPLETED/NO_SHOW로 출결을 기록한 뒤 CANCELLED로 전환된 예약은 예약 수에 포함한다. 참여 인원은 최종 Reservation.status가 아니라 보존된 attendance를 기준으로 하며, `attendance=PRESENT`만 포함하고 `attendance=ABSENT`는 제외한다. 출결 후 취소된 예약도 같은 attendance 기준을 적용한다(docs/DECISIONS.md ADR-033/ADR-040).

금액 집계에는 실제 완료된 거래만 반영한다. Payment는 PAID/PARTIAL_REFUNDED/REFUNDED 상태의 원결제 금액을 포함하고 CANCELLED는 제외한다. Refund는 `status=COMPLETED`만 포함하며 REQUESTED/CANCELLED는 제외한다. "환불 여부" 필터는 Payment.status와 별개로 `PaymentItem.refundedAmount`와 `paidAmount`를 비교해 환불 없음(0), 부분 환불(0보다 크고 paidAmount 미만), 전액 환불(paidAmount와 같음)으로 구분한다. 새로운 상태값은 추가하지 않는다(docs/DECISIONS.md ADR-040).

결제수단과 환불 여부 필터는 정가·할인금액·실결제액·환불금액·순매출의 금액 지표에만 적용한다. 예약 수와 참여 인원에는 적용하지 않으며, 운영 지표는 기간과 프로그램/클래스 조건만 따른다. 따라서 미결제 예약도 예약·참여 집계에 포함될 수 있다. 화면은 결제수단/환불 여부 필터가 금액 지표에만 적용된다는 점을 명확히 안내해야 한다(docs/DECISIONS.md ADR-041).

Phase 9 Open Questions: 없음.

---

# 15. 대시보드

## 15.1 기본 화면
운영자가 로그인했을 때 오늘의 운영 현황을 한눈에 확인한다.

## 15.2 표시 항목
- 오늘 클래스
- 클래스별 예약 인원
- 남은 자리
- 담당 선생님
- 오늘 취소 처리 건수
- 오늘 순매출

## 15.3 향후 확장
- 월 매출 그래프
- 인기 프로그램
- 재참여율
- 아이별 참여 횟수
- 친구 동반 예약 비율

## 15.4 캘린더형 홈 화면
2026-08-24 실사용 테스트에서 숫자 위젯만 있는 대시보드보다 날짜별 예약 현황을 한눈에 보는 캘린더형 홈 화면이 가장 중요하다고 확인되었다.

- 로그인 후 첫 화면(`/dashboard`)에서 날짜별 클래스/예약 현황을 확인할 수 있어야 한다.
- 날짜를 선택하면 해당 날짜의 예약자 명단을 화면 안에서 펼쳐 확인할 수 있어야 한다.
- 예약자 명단의 아이를 선택하면 아이 상세 화면으로 이동할 수 있어야 한다.
- 캘린더형 홈 화면은 15.2의 오늘 운영 현황 요약과 함께 Phase 10 범위로 다룬다.
- 캘린더 기본 보기는 월간으로 한다. 모바일에서도 월간 보기를 유지하되 날짜 셀의 정보량을 최소화한다.
- 날짜 셀에는 해당 날짜의 클래스 수와 아래 예약자 명단 포함 규칙에 따른 예약 인원을 표시하고 남은 자리는 표시하지 않는다.
- 예정 클래스와 완료 클래스는 캘린더에 포함하고 취소된 클래스는 제외한다.
- 날짜를 선택한 뒤의 상세 영역에서 클래스별 정보를 확인할 수 있어야 한다.
- Phase 10에서는 캘린더에서 새 예약을 직접 생성하지 않고 조회 및 상세 화면 이동만 제공한다. 예약 생성 workflow 확장은 범위 밖이다.

### 예약자 명단 포함 규칙
- 날짜 선택 시 예약자를 클래스별로 그룹화한다.
- RESERVED / COMPLETED / NO_SHOW 예약을 포함한다.
- 출결 없이 사전 취소된 `status=CANCELLED`, `attendance=null` 예약은 제외한다.
- 출결 기록 후 CANCELLED된 예약은 운영 이력이 있으므로 포함한다.
- 출결 후 취소된 예약은 보존된 `attendance`를 이용해 참여완료(`PRESENT`)와 노쇼(`ABSENT`) 이력을 구분한다.
- 아이 이름을 선택하면 아이 상세 화면으로 이동한다.

## 15.5 Phase 10 범위
### Scope
- 기존 정적 `/dashboard`를 실제 운영 데이터를 보여주는 관리자 홈으로 교체
- 캘린더형 날짜별 클래스/예약 현황
- 날짜 선택 후 예약자 명단 확인과 아이 상세 이동
- 15.2의 오늘 운영 현황 요약
- 모바일과 데스크톱에서 사용할 수 있는 관리자 전용 화면

### Out of Scope
- 15.3의 월 매출 그래프, 인기 프로그램, 재참여율, 아이별 참여 횟수, 친구 동반 예약 비율
- Phase 10에서 새 결제·환불·매출 계산 정책을 만드는 일
- 캘린더에서 새 예약을 직접 생성하는 workflow
- 시스템이 특정 예약을 "환불 처리 필요"로 자동 판정하는 기능
- 예약자용 고객 화면, 문자/카카오 알림, 날씨 자동 연동

## 15.6 오늘 운영 지표
### 예약 현황
- 오늘 KST에 열리는 클래스의 현재 운영 예약 건수를 집계한다.
- RESERVED / COMPLETED / NO_SHOW 및 출결 후 CANCELLED 예약을 포함한다.
- 출결 없이 사전 취소된 CANCELLED 예약은 제외한다.
- 가능하면 예약 인원과 클래스 정원을 함께 표현한다.

### 오늘 취소 처리 건수
- `Reservation.cancelledAt`이 오늘 KST에 속하는 예약 건수를 집계한다.
- 해당 예약의 클래스 날짜가 오늘인지 여부와 무관한 "오늘 취소 처리된 건수" 운영 활동 지표다.

### 오늘 순매출
- 화면 라벨은 "오늘 매출"이 아니라 "오늘 순매출"을 사용한다.
- ADR-040의 거래 발생일 귀속 원칙을 그대로 적용해, `Payment.paidAt`이 오늘 KST에 속하는 `PaymentItem.paidAmount` 합계에서 `Refund.refundedAt`이 오늘 KST에 속하고 `status=COMPLETED`인 Refund 합계를 뺀다.
- 과거 결제에 대한 환불을 오늘 처리했다면 오늘 순매출에서 차감한다.
- 가능하면 오늘 결제금액과 오늘 환불금액을 보조 정보로 각각 표시한다.

## 15.7 환불 처리 필요 건
Phase 10에서는 "환불 처리 필요 건"을 제공하지 않는다. ADR-035/ADR-039에 따라 예약 취소와 환불은 독립적이며, 현재 정책만으로 시스템이 특정 예약을 환불 필요 대상으로 자동 판정할 수 없다. `Reservation.status=CANCELLED`이고 미환불 금액이 남았다는 이유만으로 환불 필요 건으로 판단하지 않는다. 향후 환불 필요 여부를 명시적으로 관리하는 정책 또는 상태가 확정되면 별도 Phase에서 재검토한다.

## 15.8 Phase 10 Open Questions
없음(docs/DECISIONS.md ADR-042).

---

# 16. 검색 및 필터

## 아이
- 이름
- 연락처
- 상태

생년월일 검색은 제공하지 않는다(정확한 날짜를 기억해서 검색하는 실사용 사례가 없어 제거했다 — docs/DECISIONS.md ADR-016 참고).
기본 노출/펼침 등 검색 UI 동작은 21-1장을 따른다.

## 클래스
- 날짜(기간): 직접 입력과 오늘/이번 주/이번 달 프리셋
- 상태: 진행 예정/종료를 포함한 기존 상태 필터

프로그램/장소/선생님 검색은 제공하지 않는다(현재 개수가 적어 목록을 훑는 것이 더 빠르고, 실사용으로 뒷받침되지 않는 검색 조건은 먼저 만들지 않는다 — docs/DECISIONS.md ADR-021 참고, ADR-016과 같은 맥락). 날짜 검색은 특정 하루가 아니라 기간(시작일~종료일) 검색이다.

클래스 상태 필터의 "예정"/"완료"는 DB status뿐 아니라 클래스 endsAt과 현재 시각도 함께 비교한다(8.6, docs/DECISIONS.md ADR-028 참고).

오늘/이번 주/이번 달은 기존 KST 날짜 규칙을 사용하고, 이번 주는 월요일부터 일요일까지이며 시작일과 종료일을 모두 포함한다(ADR-040의 기간 규칙 재사용). 기간 프리셋과 상태 필터는 기존 날짜+상태 검색처럼 함께 적용할 수 있다. 기본값은 ADR-022대로 날짜 제한 없이 진행 예정(SCHEDULED)이며, 날짜 프리셋은 사용자가 선택하기 전에는 적용하지 않는다.

## 예약
- 아이
- 클래스
- 상태
- 기간

예약 상태 필터의 "예약됨"/"완료"는 DB status뿐 아니라 소속 클래스의 endsAt과 현재 시각도 함께 비교한다(10.3, docs/DECISIONS.md ADR-028 참고).

## 매출
- 기간
- 프로그램
- 결제수단
- 환불 여부

기간과 프로그램 조건은 운영 지표(예약 수·참여 인원)와 금액 지표 모두에 적용한다. 결제수단과 환불 여부는 금액 지표에만 적용하며, 예약 수·참여 인원의 의미를 바꾸지 않는다(docs/DECISIONS.md ADR-041).

---

# 17. 계정 및 권한

## 17.1 역할과 계정 연결
- 역할은 기존 `ADMIN`, `TEACHER` 두 개만 사용한다. `OWNER`, `MANAGER`, 사용자별 custom permission은 도입하지 않는다.
- `User`는 로그인 계정이고 `Teacher`는 수업 배정용 운영 데이터다.
- `TEACHER` 역할의 User는 기존 활성 Teacher 한 명과 반드시 1:1로 연결한다.
- 한 Teacher를 여러 User에 연결하거나 한 TEACHER User를 여러 Teacher에 연결할 수 없다.
- ADMIN은 Teacher 연결 없이 존재할 수 있다.
- TEACHER는 `User.isActive=true`이고 연결된 `Teacher.isActive=true`일 때만 접근할 수 있다. 둘 중 하나라도 비활성이면 변경 완료 후 다음 보호된 서버 요청부터 접근을 차단한다.

## 17.2 ADMIN 권한
- ADMIN은 기존과 동일하게 전체 운영 기능에 접근할 수 있다.
- ADMIN만 내부 계정을 만들고 계정의 이름, 이메일, 역할, 활성 상태를 변경하며 비밀번호를 재설정할 수 있다.
- User 물리 삭제는 허용하지 않는다.
- 본인 계정 비활성화와 본인의 ADMIN→TEACHER 강등은 허용하지 않는다.
- 마지막 활성 ADMIN의 비활성화와 강등은 허용하지 않으며, 항상 최소 한 명의 활성 ADMIN을 유지한다.

## 17.3 TEACHER 권한 매트릭스

| 기능 | ADMIN | TEACHER |
|---|---|---|
| Dashboard | 전체 운영·재무 지표 조회 | 본인 배정 클래스에 필요한 운영 정보만 조회. 재무·Revenue 지표 금지 |
| 아이 목록·전체 아이 상세 | 조회 | 금지 |
| 아이 등록·수정 | 가능 | 금지 |
| 배정 클래스 참가자 | 모든 클래스의 아이 이름·보호자 연락처·안전정보 조회 | 본인 배정 클래스 참가자의 아이 이름·보호자 연락처·ChildSafetyInfo 조회 |
| ChildSafetyInfo 수정 | 가능 | 금지 |
| 동의 조회·변경 관리 | 가능 | 금지 |
| 프로그램 관리 | 조회·생성·수정 | 관리 화면 접근 금지. 본인 배정 클래스에 필요한 프로그램 정보만 조회 |
| 선생님 관리 | 조회·생성·수정 | 전체 관리 화면 금지. 본인 Teacher 정보와 같은 배정 클래스의 다른 Teacher 이름만 조회 |
| 클래스 조회 | 전체 클래스 | `ClassTeacher`로 본인에게 배정된 클래스만 |
| 클래스 생성·반복 생성·수정·취소 | 가능 | 금지 |
| 예약 조회 | 전체 예약 | 본인 배정 클래스 운영에 필요한 참가자 예약 상태만 조회 |
| 예약 생성·취소 | 가능 | 금지 |
| 출결 기록 | 모든 클래스 | 본인 배정 클래스만 |
| 결제·결제 금액·환불 | 조회·처리 | 금지 |
| Revenue·재무 화면 | 조회 | 금지 |
| 계정 관리 | 조회·생성·수정·활성 변경·비밀번호 재설정 | 금지 |
| 본인 비밀번호 변경 | 가능 | 가능 |

## 17.4 권한 강제
- Navigation과 UI는 권한 없는 메뉴와 액션을 숨기지만, 메뉴 숨김을 인가로 간주하지 않는다.
- 직접 URL, server query, Server Action에서 서버가 현재 로그인 계정의 최신 역할과 활성 상태를 최종 검증한다.
- TEACHER의 권한은 서버가 현재 User와 연결된 Teacher 및 `ClassTeacher` 배정을 해석해 판정한다. 클라이언트가 제출한 userId, teacherId, role은 권한 근거로 신뢰하지 않는다.
- TEACHER는 본인 배정 ClassSchedule을 통해서만 참가자의 연락처와 ChildSafetyInfo 접근 권한을 얻는다. 무관한 아이는 직접 URL이나 query 호출도 차단한다.
- 계정·Teacher 비활성화, 역할 변경, 비밀번호 재설정은 이미 처리 중인 요청을 중단하지 않지만 변경 완료 후 다음 보호된 서버 요청부터 기존 세션에도 즉시 적용한다. JWT에 저장된 과거 role/isActive만으로 인가를 계속하지 않는다.
- 최초 로그인 또는 ADMIN 비밀번호 재설정 후 비밀번호 변경이 필요한 계정은 비밀번호 변경 화면 외 운영 기능에 접근할 수 없다.

---

# 18. 데이터 무결성

다음 조건은 서버/DB에서 모두 보호하는 것을 원칙으로 한다.

- 동일 아이의 동일 클래스 중복 예약 금지
- 클래스 정원은 1~99명의 정수여야 하고 기본값은 8명이다. DB는 1명 이상을, application validation은 99명 이하를 보호한다.
- 관리자 초과 예약은 명시적 확인 후 허용하되, 클래스 행 잠금과 최신 RESERVED 예약 수 서버 재검증 없이 우회 생성할 수 없다(ADR-023, ADR-043, ADR-044).
- 자기 자신과 친구관계 생성 금지
- 동일 관계 중복 금지
- 동일 클래스에 동일 선생님 중복 배정 금지
- 취소된 클래스 신규 예약 금지
- 환불금액이 결제금액을 초과하지 않도록 검증
- 음수 금액 방지
- 삭제로 과거 매출/예약 이력이 사라지지 않도록 한다.

---

# 19. 감사/운영 이력

중요한 변경에는 처리자와 처리일시를 남긴다.

대상:
- 클래스 취소
- 예약 취소
- 환불
- 주요 고객정보 변경
- 매출 관련 수정
- 계정 생성, 이메일 변경, 역할 변경, 활성/비활성 변경, 비밀번호 재설정

Phase 16 계정 변경 이력은 append-only로 남긴다. 대상 User, 변경 종류, 처리한 ADMIN, 처리 시각과 필요한 경우 최소한의 이전/이후 비민감 값을 기록한다. 평문 비밀번호, password hash, token, session 값은 기록하지 않는다.

Phase 16의 계정 변경 이력은 계정 관리에 한정한다. 전체 시스템의 범용 Audit Log는 향후 별도 범위로 확장한다.

---

# 20. 개인정보 및 보안

- 최소한의 개인정보만 저장한다.
- 연락처 등 개인정보를 일반 로그에 출력하지 않는다.
- 인증되지 않은 사용자는 운영 데이터를 볼 수 없다.
- 서버에서 권한을 검증한다.
- 보호된 요청은 현재 DB의 계정·역할·Teacher 연결과 활성 상태를 기준으로 권한을 판정한다.
- 비밀번호는 hash만 저장하며 기존 비밀번호, 평문 비밀번호, password hash를 조회 화면이나 로그에 노출하지 않는다.
- secrets는 환경변수로 관리한다.
- DB 접근 권한을 최소화한다.

---

# 20-1. 아동 안전 정보 및 동의

## 20-1.1 배경
야외에서 운영하는 프로그램이므로 응급상황 대응에 필요한 최소 정보를 보유한다.
다만 알레르기 등 건강 관련 정보는 민감정보에 해당하므로 일반 고객정보와 분리하여 관리한다.

## 20-1.2 아동 안전 정보
- 알레르기 및 주의사항
- 응급 시 유의사항(경련, 천식 등 즉시 알아야 할 사항에 한정)
- 비상연락처 이름
- 비상연락처 전화번호
- 비상연락처 관계

수집 범위는 "현장에서 즉시 대응하는 데 필요한 것"으로 한정한다.
진단명, 병력, 복용약 이력 등 대응에 불필요한 정보는 수집하지 않는다.

## 20-1.3 저장 및 접근 규칙
- 아동 기본정보와 별도 테이블에 저장한다.
- 아이 목록 조회에는 포함하지 않는다. 상세 화면에서만 조회한다.
- 로그, 에러 메시지, 외부 전송에 절대 포함하지 않는다.
- 조회/수정 시 처리자와 일시를 남긴다.

## 20-1.4 동의 관리
다음 동의 여부와 동의 일시를 기록한다.

- 개인정보 수집·이용 동의 (필수)
- 민감정보(건강 관련) 수집 동의 (필수, 별도 동의)
- 활동 사진 촬영 및 보호자 공유 동의 (필수)
- 활동 사진의 홍보·마케팅 활용 동의 (선택)

홍보 활용 동의가 없는 아이의 사진은 외부 공개 자료에 사용하지 않는다.
동의는 철회할 수 있으며, 철회 후 재동의도 가능하다.
동의/철회는 이력으로 누적 기록한다. 기존 기록을 덮어쓰지 않는다.
현재 동의 상태는 가장 최근 기록으로 판단한다.

## 20-1.5 클래스 단위 안전 정보
- 해당 클래스의 보험 가입 여부
- 보험사 / 증권번호
- 활동 장소의 안전 특이사항 메모

## 20-1.6 당일 운영
- 클래스 상세 화면에서 참여 아이의 알레르기/비상연락처를 한 화면에 확인할 수 있어야 한다.
- 출결(참여/불참)을 현장에서 기록할 수 있어야 한다.

---

# 21. 화면 구성 초안

```text
로그인

대시보드
├── 오늘의 클래스
├── 예약 현황
├── 매출
└── 처리 필요

아이 관리
├── 아이 목록
├── 아이 등록
└── 아이 상세
    ├── 기본정보
    ├── 친구
    ├── 형제/자매
    ├── 예약 이력
    ├── 결제 이력
    └── 환불 이력

프로그램 관리
├── 프로그램 목록
├── 프로그램 등록
└── 프로그램 상세

클래스 일정
├── 캘린더
├── 클래스 목록
└── 클래스 상세
    ├── 참여 아이
    ├── 선생님
    ├── 예약
    └── 취소

예약 관리
├── 예약 목록
├── 예약 등록
└── 예약 상세

매출 관리
├── 매출 대시보드
├── 결제
├── 환불
└── 기간별/프로그램별 집계

선생님 관리
└── 선생님 목록

계정 관리 (ADMIN)
├── 계정 목록
├── 계정 생성
├── 계정 정보·역할·활성 상태 변경
└── 임시 비밀번호 재설정

내 계정
└── 비밀번호 변경

선생님 운영 (TEACHER)
├── 본인 배정 클래스 운영 현황
├── 본인 배정 클래스 상세·참가자
├── 참가자 연락처·안전정보 조회
└── 출결 기록
```

---

# 21-1. UI 설계 원칙 (모바일 우선)

## 21-1.1 배경
운영자는 야외 프로그램 현장에서 폰으로 아이 정보를 확인하고 보호자에게 바로 전화를 거는 경우가 많다. 초기 화면 설계(3.3)는 아이 목록에 8개 항목을 한 번에 나열하는 방식이었는데, 이는 폰 화면뿐 아니라 데스크톱에서도 정보 밀도가 높아 읽기 어렵다는 문제가 있었다. 이 문제는 아이 목록에 국한되지 않고 향후 클래스/예약/매출 등 다른 목록 화면에도 동일하게 반복될 구조적 문제다.

요건 정의 초기에는 "3.3의 8개 항목 조회"와 "모바일 가독성" 요구가 서로 충돌하는 것으로 보였으나, 3.3 자체를 개정하여 해소했다(더 이상 충돌 아님. docs/DECISIONS.md ADR-013 참고).

## 21-1.2 공통 원칙
아래 원칙은 화면 크기와 무관하게 목록/조회 화면 전반에 적용된다.

- 목록은 식별에 필요한 최소 항목만 기본 노출하고, 나머지는 상세 화면에서 확인한다.
- 검색/필터 영역은 기본적으로 최소한만 노출하고, 자주 쓰지 않는 조건은 접어두고 필요 시 펼치는 방식으로 제공한다.
- 실사용으로 뒷받침되지 않는 검색 조건(예: 정확한 날짜를 기억해서 검색하는 패턴)은 제공하지 않는다.
- 전화번호가 입력되어 있으면 탭(클릭) 한 번으로 통화를 걸 수 있도록 tel: 링크로 표시한다.
- 좁은 화면에서는 상단 가로 네비게이션 대신 햄버거 메뉴 + 좌측 드로어 방식을 기본으로 한다.

## 21-1.3 아이 목록 적용
- 목록 기본 노출 항목: 이름, 보호자 연락처, 다음 예약 (3.3 참고). 나머지 항목은 아이 상세(3.4)에서 확인한다.
- 보호자 연락처는 입력되어 있으면 tel: 링크로 표시한다.
- 오늘/이번주 예약이 있는 아이를 목록에서 배지로 구분할 수 있는 자리를 둔다. 다만 예약(Reservation) 데이터는 Phase 5에서 구현되므로, 그 전까지는 정렬 UI/다음 예약 컬럼/배지 자리 등 구조만 두고 값은 비워둔다(22장 Phase 5, docs/DECISIONS.md ADR-012/ADR-015 참고).
- 검색 영역은 기본적으로 상태 필터만 노출한다. 이름/보호자 연락처 검색어 입력은 기본 접힘 상태이며 "검색" 버튼을 눌러야 펼쳐진다. 생년월일 검색은 제공하지 않는다(16장, docs/DECISIONS.md ADR-016 참고).

## 21-1.4 적용 범위와 후속 작업
- 이 원칙은 이미 배포된 Phase 2(아이 관리) PR을 되돌리지 않고, 별도 후속 작업으로 반영한다(docs/DECISIONS.md ADR-014 참고).
- 실제 UI 컴포넌트/패턴(정렬 UI 세부 동작, 카드/테이블 전환 기준 등 구현 방식)은 요건이 아니라 설계 결정이므로, architect가 docs/UI-GUIDELINES.md에 설계 산출물로 정리하고 developer가 build 단계에서 문서와 구현을 만든다.
- 클래스/예약/매출 등 다른 목록 화면에도 21-1.2의 공통 원칙을 순차 적용한다. 화면별 기본 노출 항목은 각 화면 설계 시점에 별도로 확정한다.

---

# 22. 개발 우선순위

원칙: **Phase 5까지 완료하면 배포해서 실제 운영에 쓰기 시작한다.**
결제/매출/대시보드는 실사용을 시작한 뒤에 붙인다.
전체를 다 만들고 한 번에 오픈하지 않는다.

## Phase 1 — 초기화
- Next.js / TypeScript / Tailwind / shadcn/ui
- Prisma + Supabase 연결
- Auth.js 로그인 (운영자 계정)
- Vitest / Playwright 환경
- Vercel 배포 파이프라인까지 이 단계에서 연결한다

## Phase 2 — 아이 관리
- CRUD
- 검색
- 상세 화면

## Phase 3 — 프로그램 / 선생님
- 프로그램 CRUD
- 선생님 CRUD
- 클래스를 만들기 위한 선행 조건

### 착수 전 확인
- `lib/children/age.ts` `calculateAge()` 가 로컬 타임존 기준 "오늘"과 `birthDate`(UTC 자정 기준 `@db.Date`)를 직접 비교한다. 지금은 Phase 2 상세/목록에 연령 "표시"용으로만 쓰여 문제가 드러나지 않았지만, Program.targetAgeMin/targetAgeMax 대상 연령 판정처럼 나이를 조건으로 쓰는 로직이 생기면 타임존에 따라 생일 경계에서 하루 어긋날 수 있다. Phase 3~5 설계 단계에서 나이를 판정 조건으로 쓰기 전에 이 함수의 타임존 처리를 재검토하고 회귀 테스트를 추가할 것 (Phase 2 리뷰 MINOR).

## Phase 4 — 클래스 일정
- 클래스 생성/수정
- 정원 (기본 8명)
- 선생님 2명 배정
- 클래스 전체 취소 + 취소 사유

## Phase 5 — 예약
- 아이 단위 예약 생성
- 개인 예약 취소 + 취소 사유
- 정원 검증
- 중복 예약 방지

10.5(친구와 함께 예약)는 Phase 5 범위가 아니다. Phase 7(관계)에서 다룬다 — 22장 Phase 7 목록에 이미 "친구와 함께 예약"이 명시되어 있어 문서 자체는 원래 일관돼 있었다. Phase 5 architect 설계 단계에서 이 사실을 재확인했을 뿐이며, Phase 5는 10.5를 구현하지 않는다.

### 착수 전 확인
- 예약 목록의 기본 상태 필터/정렬은 "목록 기본 필터는 지금 처리해야 할 것만 보여준다"는 원칙(8.6, docs/DECISIONS.md ADR-022)을 따라 결정한다. 클래스 목록과 동일하게 예약도 시간이 지나면 상태가 자동으로 바뀌는 도메인이므로, 임의로 다른 기준을 새로 만들지 않는다.
- 정원 동시성 제어, 취소 후 재예약 처리, 비활성 아이 예약 차단 방식은 각각 docs/DECISIONS.md ADR-023 / ADR-024 / ADR-025 를 따른다.

### ★ 여기서 배포하고 실제 운영에 사용한다
아이 / 클래스 / 예약이 되면 기존 수기 관리를 대체할 수 있다.
이후 Phase 는 실사용 중에 하나씩 붙인다.

## Phase 6 — 안전 정보 / 출결
- 아동 안전 정보 (알레르기, 비상연락처)
- 동의 관리
- 클래스 보험 정보 (ADR-032: 직전 클래스 값 불러오기 편의 기능 포함)
- 당일 출결 기록 — 출결 기록 시 Reservation.status를 PRESENT→COMPLETED, ABSENT→NO_SHOW로 함께 전환한다(ADR-031). ClassSchedule.status의 COMPLETED 전환은 이번 Phase의 범위가 아니다(계속 ADR-026대로 endsAt 기준 조회 시점 계산).
- 클래스 상세에서 참여 아이 안전 정보 확인

## Phase 7 — 관계
- 친구 관계
- 형제/자매 관계
- 친구와 함께 예약

## Phase 8 — 결제 / 환불
- 결제 등록 (Payment + PaymentItem)
- 아이별 환불
- 부분 환불 — 금액은 관리자 자유 입력으로 하며, 결제금액을 초과할 수 없다(13.4, docs/DECISIONS.md ADR-007/ADR-036)
- 환불 사유
- 출결 기록된 예약(COMPLETED/NO_SHOW)의 취소 허용 — 취소 시 `Reservation.status`를 `CANCELLED`로 직접 전환하고, `attendance`/`attendanceRecordedById`/`attendanceRecordedAt`은 이력으로 보존한다(ADR-033)
- 노쇼(NO_SHOW) 예외 환불 — 관리자 재량 + 사유 필수 기록으로 허용하며, 별도의 사유 화이트리스트나 승인자 지정 절차(결재선)는 두지 않는다(ADR-034)
- 예약 취소와 환불은 별개 동작 — 출결 기록 후 예약을 취소해도 환불을 자동으로 트리거하지 않는다. 환불은 관리자가 결제/환불 화면에서 별도로 수동 처리한다(ADR-035)
- 환불 승인 절차 — 부분 환불, 전액 환불, 노쇼 예외 환불을 포함한 모든 환불은 관리자 1인이 별도 승인 단계나 결재선 없이 처리한다. 현재 1인 운영 규모에 맞춘 결정이며, 직원 증가나 운영 규모 확대 시 재검토한다(ADR-037)
- 할인금액 입력 — 관리자가 0 이상 정가 이하의 금액을 자유 입력하고, 실제 결제금액은 정가에서 할인금액을 빼서 계산한다. 별도 할인 정책 엔진은 만들지 않는다(ADR-038)
- 일반 환불 자격 — 결제와 남은 환불 가능 금액이 있으면 예약 상태와 무관하게 수동 환불을 허용한다. 클래스 전체 취소 건도 같은 흐름을 사용하며 자동 환불은 하지 않는다(ADR-039)

### 착수 전 확인
2026-08-26 실사용 테스트 중 확인된 5개 Open Question과 Phase 8 설계 중 추가로 확인된 2개 blocker는 모두 사용자가 직접 결정해 해소되었다(위 Phase 8 목록과 docs/DECISIONS.md ADR-033~039 참고) — 출결 기록된 예약(COMPLETED/NO_SHOW)의 취소 허용 및 상태 전환 방식, 노쇼 예외 환불의 허용 조건, 취소-환불 자동 트리거 여부, 부분 환불 금액 계산 방식, 환불 승인 절차, 할인금액 입력 정책, 일반 환불 자격.

Phase 8 Open Questions: 없음.

참고로 결제수단(12.4)과 결제상태(12.3) enum, 부분 환불의 결제금액/환불금액 분리 관리 원칙(13.4), 환불 상한 동시성 제어(PaymentItem.refundedAmount + 행 잠금, docs/DECISIONS.md ADR-007)는 이미 확정되어 있어 Open Question이 아니다.

## Phase 9 — 매출
- 기간별
- 프로그램별
- 클래스별
- 순매출

### 착수 전 확인
총 결제금액/순매출 계산식, 지표별 기간 귀속, 늦은 환불 처리, 예약·참여 인원 포함 범위, 거래 상태별 집계, 기간 프리셋 규칙을 사용자가 직접 확정했다(14장, docs/DECISIONS.md ADR-040). 예약/참여는 클래스일, 결제/할인은 결제일, 환불은 환불 처리일을 기준으로 하며 서로 다른 시간축을 하나로 합치지 않는다. 설계 단계에서 확인된 마지막 blocker인 결제수단/환불 여부 필터의 적용 범위도 금액 지표로 한정하기로 확정했다(docs/DECISIONS.md ADR-041).

Phase 9 Open Questions: 없음.

## Phase 10 — 대시보드
- 오늘의 클래스
- 예약 현황
- 오늘 취소 처리 건수
- 오늘 순매출

**실사용 테스트 피드백 (사장님, 2026-08-24):** 15.1~15.2의 숫자 위젯형 대시보드와 별개로, "첫 화면에서 날짜별 예약현황을 한눈에 보고, 날짜를 클릭하면 그날 예약자 명단이 드롭다운으로 뜨고, 명단의 아이를 클릭하면 아이 상세로 이동"하는 **캘린더형 홈 화면**이 가장 중요하다고 확인되었다. Phase 10에서는 월간 캘린더와 조회/상세 이동만 제공하고, 캘린더에서 새 예약을 직접 생성하는 workflow는 포함하지 않는다(15.4, docs/DECISIONS.md ADR-042).

### 착수 전 확인
현재 `/dashboard`는 Phase 1 완료 문구만 표시하는 정적 화면이며 운영 데이터를 조회하지 않는다. 월간 캘린더, 상태별 클래스/예약 포함 범위, 오늘 운영 지표, 오늘 순매출, 환불 처리 필요 건 제외 여부를 사용자가 직접 확정했다(15.4~15.7, docs/DECISIONS.md ADR-042).

Phase 10 Open Questions: 없음.

## Phase 11 — 안정화

### Problem
- Phase 1~10의 핵심 기능은 구현되었지만, 현재 CI는 lint/unit test/build만 실행하고 Playwright E2E는 로컬 수동 실행에 의존한다(ADR-009).
- 기존 E2E는 인증, 종료 클래스 lifecycle, Phase 6 비인증 접근 경계, 결제/환불, 매출, 대시보드를 다루지만, 인증된 관리자가 아이를 등록·수정하고 안전정보·예약·출결로 이어가는 핵심 운영 흐름은 브라우저 수준 회귀 테스트가 부족하다.
- 현재 사용자가 확인한 실제 성능/운영 문제는 없다. 근거 없는 최적화나 전면 리팩터링 없이 확인된 E2E 공백을 우선 보강한다.

### Current Process
- GitHub Actions의 `quality` job에서 lint, Vitest unit test, Next.js build를 실행한다.
- Playwright E2E는 실제 DB와 인증 흐름을 사용하는 로컬 수동 검증이며 CI에는 포함하지 않는다(ADR-009).
- 기존 6개 Playwright spec의 23개 시나리오는 다음을 검증한다.
  - `auth.spec.ts`: 미인증 `/dashboard` 접근 차단
  - `class-ended-lifecycle.spec.ts`: 완료 클래스 필터·상세 버튼 숨김·종료 클래스 예약 생성 서버 차단
  - `phase6-access.spec.ts`: 미인증 안전정보/클래스 참여자 화면 접근 차단
  - `payment-refund-lifecycle.spec.ts`: 결제·환불·NO_SHOW 취소 후 출결 보존과 자동 환불 방지
  - `revenue-report.spec.ts`: ADR-040/041 매출 집계와 기간·필터·모바일 표시
  - `dashboard.spec.ts`: ADR-042 월간 캘린더·상태 집계·오늘 지표·모바일 7열
- 아이 등록/수정은 unit test로 검증되지만 실제 폼 제출 E2E가 없고, 안전정보는 비인증 접근 E2E만 존재한다. 정상 예약 생성·조회와 PRESENT/ABSENT 출결 기록도 인증된 브라우저 흐름의 E2E가 없다.
- 아이와 예약은 hard delete 대신 비활성화/상태 전환을 사용하며, 애플리케이션 코드가 hard delete를 호출하지 않는다는 unit test가 이미 존재한다. Phase 11에서 삭제 E2E를 새로 추가할 근거는 없다.

### User Story
- 운영 관리자로서 아이 등록·수정부터 기존 안전정보 확인, 예약 생성·조회, 출결 처리까지 이어지는 핵심 흐름이 변경 후에도 깨지지 않았음을 배포 전에 확인하고 싶다.
- 운영 관리자로서 실제로 재현되는 성능 문제가 있을 때만 동일 조건의 전후 측정 근거를 바탕으로 개선되기를 원한다.

### Scope
- 아이 등록 폼 제출과 생성된 아이 상세 확인 E2E
- 아이 정보 수정과 수정 결과 유지 확인 E2E
- 해당 아이에게 이미 저장된 기존 안전정보/주의정보가 아이 상세 및 클래스 참여자 운영 화면에서 올바르게 표시되는 흐름
- 해당 아이의 정상 예약 생성과 예약 목록/상세 조회 연결 흐름
- 종료된 클래스의 RESERVED 예약에 대한 PRESENT/ABSENT 출결 처리와 각각 COMPLETED/NO_SHOW 상태 전환
- 출결 이후 기존 예약 상태, 출결 이력, 취소·환불 분리 등 기존 도메인 규칙이 유지되는지에 대한 필요한 회귀 검증. 이미 `payment-refund-lifecycle.spec.ts`가 충분히 검증하는 NO_SHOW 취소·출결 보존·자동 환불 방지 시나리오는 중복 작성하지 않는다.
- 위 흐름의 인증·권한·개인정보 노출 규칙 회귀 검증
- 위 E2E 보강 과정에서 실제로 재현 가능한 성능/운영 문제가 발견된 경우에 한한 전후 측정과 개선

### Out of Scope
- 모든 관리자 화면을 포괄하는 E2E 작성
- 이미 충분한 E2E가 있는 결제/환불/매출/대시보드 흐름의 단순 중복 테스트
- 프로그램/선생님 CRUD, 관계 관리 등 이번에 필수로 선택하지 않은 흐름의 E2E 확대
- 아이/예약 hard delete 등 현재 제품에 없는 파괴적 CRUD 추가 또는 그 E2E 작성
- 새로운 고객 기능이나 관리자 업무 기능
- 예약/결제/환불 정책, 개인정보 수집 항목, 안전정보 정책 또는 상태 모델 변경
- 실제 재현 근거나 측정 없이 Dashboard/Revenue를 포함한 화면·쿼리를 최적화하거나 전면 리팩터링하는 작업
- 절대 응답시간 SLA를 임의로 설정하는 작업
- 독립 CI 테스트 DB 구축과 Playwright GitHub Actions required check 활성화
- 24장의 미결정 백로그를 사용자 선택 없이 Phase 11 범위로 편입하는 작업
- 테스트 통과만을 목적으로 assertion 또는 서버 도메인 검증을 완화하는 작업

### Business Rules
- 기존 REQUIREMENTS와 Accepted ADR의 동작을 회귀 기준으로 사용하며 Phase 11에서 임의로 변경하지 않는다.
- 새로운 개인정보 항목과 안전정보 정책을 추가하지 않고 기존 수집·노출 범위만 검증한다.
- 예약과 출결의 기존 상태 의미 및 전환 규칙을 변경하지 않는다.
- Playwright는 ADR-009에 따라 계속 로컬 수동 검증으로 실행하고 GitHub Actions required check로 추가하지 않는다.
- 독립 CI 테스트 DB가 제공되기 전에는 Playwright CI 실행을 도입하지 않는다. 향후 도입 시에도 Production DB와 ADR-030의 shared Preview DB를 사용하지 않고, 독립 테스트 DB·결정적인 seed·실행 간 격리·명시적 정리 원칙이 먼저 확정되어야 한다.
- 운영 DB, Production 데이터 또는 shared Preview 데이터를 자동화 테스트 데이터로 사용하지 않는다.
- 테스트 데이터와 로그에 실제 개인정보나 secrets를 포함하지 않는다.
- 추가 E2E는 고정 sleep이나 브라우저별 CSS 문자열 표현에 의존하지 않고, 사용자가 확인할 실제 상태·DOM·layout 조건을 retry 가능한 assertion으로 검증한다.
- 실제 성능 문제가 재현되지 않으면 성능 관련 제품 코드를 수정하지 않는다.
- 성능 문제를 개선할 때는 동일한 테스트 데이터·실행 조건·요청/화면에서 변경 전후를 비교한다. 재현 조건, 개선 전 측정값, 개선 후 측정값, 가능한 경우 query 수 또는 반복 호출 수, 기능·도메인 회귀 여부를 기록한다.
- 실제 baseline이 없으므로 절대적인 밀리초 기준을 임의로 만들지 않는다. 성능 문제가 발견되지 않으면 성능 완료 기준은 적용 대상 없음으로 본다.

### Acceptance Criteria
- 인증된 관리자가 아이를 등록하고 상세 화면에서 생성 결과를 확인할 수 있다.
- 같은 아이의 정보를 수정하고 상세/목록에서 수정 결과가 유지되는 것을 확인할 수 있다.
- 기존 안전정보/주의정보가 아이 상세와 해당 아이가 예약된 클래스의 참여자 운영 화면에 정확히 표시되며, 목록이나 불필요한 화면에는 민감정보가 새로 노출되지 않는다.
- 해당 아이를 기존 예약 규칙에 따라 예약하고 예약 목록/상세에서 같은 아이와 클래스로 연결된 결과를 확인할 수 있다.
- 종료된 클래스의 서로 다른 RESERVED 예약에 PRESENT와 ABSENT를 기록하면 각각 Reservation.status가 COMPLETED와 NO_SHOW로 전환되고 attendance 이력이 보존·표시된다.
- 출결 이후 취소·환불 분리, 상태별 허용/차단 등 기존 도메인 규칙이 기존 unit/E2E와 함께 회귀 없이 유지된다.
- 추가한 E2E는 독립적이고 deterministic하며 반복 실행에서 통과한다. 고정 sleep과 취약한 CSS 문자열 의존을 새로 만들지 않는다.
- 기존 전체 Playwright E2E, unit test, lint, typecheck, build가 통과한다.
- 인증/권한 검증이 약화되지 않고 실제 개인정보나 secrets가 테스트 데이터·로그에 포함되지 않는다.
- 재현 가능한 성능 문제가 발견되면 변경 전후 근거를 기록하고 기능·도메인 회귀가 없음을 검증한다. 발견되지 않으면 불필요한 성능 코드 변경 없이 적용 대상 없음으로 완료할 수 있다.
- GitHub Actions에 Playwright required check 또는 독립 CI DB 구축을 추가하지 않는다.

### Conflicts
- 없음. Playwright를 로컬 수동 실행으로 유지하는 결정은 ADR-009와 일치하며, shared Preview DB를 CI 테스트에 사용하지 않는 조건은 ADR-030과 충돌하지 않는다. 상태 기반 UI와 서버 검증의 회귀를 의미 있는 E2E로 보강하는 범위는 ADR-028/ADR-029의 기존 방향을 따른다.

### Open Questions
없음.

### Priority
Now — Phase 1~10 기능이 모두 병합된 뒤 로드맵에 남은 첫 번째 미완료 Phase이며, 새 기능 확장 전에 기존 운영 흐름의 회귀 방지와 측정 가능한 안정화 기준을 확정해야 한다.

## Phase 12 — 클래스·예약 운영 고도화

### Problem
- 모바일 운영 중 클래스 목록과 상세에서 잔여석, 예약 상태, 출결, 취소, 결제 여부를 한 번에 파악하기 어렵다.
- 클래스 목록은 날짜/프로그램/상태만 보여주고, 상세는 현재 예약·출결·안전정보가 한 섹션에 나열되어 일반 예약과 취소 이력을 빠르게 구분하기 어렵다.
- 현재 만석 클래스는 예약 후보에서 제외되고 서버도 예약 생성을 차단해, 관리자가 운영상 필요한 초과 예약을 처리할 수 없다.
- `ClassSchedule.capacity`는 이미 클래스별로 저장되지만 서버 검증은 8명을 상한으로 강제하고 있어, "기본 정원 8명·클래스별 조정·관리자 초과 예약"이라는 이번 운영 요구와 일부 충돌한다.

### Current Process
- `ClassSchedule.capacity`는 기본값 8인 필수 정수 컬럼이고 DB CHECK는 1 이상만 보장한다. 모든 기존 클래스가 이미 값을 가지므로 정원 저장을 위한 새 컬럼이나 backfill은 필요하지 않다(ADR-004).
- 클래스 생성/수정 서버 검증과 폼은 현재 1~8명만 허용한다. 예약 생성은 클래스 행을 잠근 뒤 `status=RESERVED`인 예약 수가 capacity 이상이면 하드 차단하며, 만석 클래스는 후보 목록에서도 제외한다(ADR-023).
- 클래스 상세에는 아이별 예약 상태와 출결, 안전정보가 표시되지만 결제 여부는 포함되지 않고, 일반 예약과 취소 예약도 별도 운영 그룹으로 나뉘지 않는다.
- 클래스 상세의 "예약 추가"와 아이 상세의 "예약 추가"는 모두 같은 `/reservations/new` 화면을 각각 `classScheduleId` 또는 `childId`로 미리 선택해 사용한다. 실제 생성은 같은 Server Action과 검증을 거친다.
- 결제는 예약당 선택적 `PaymentItem` 1건으로 연결되고 Payment.status는 PAID/PARTIAL_REFUNDED/REFUNDED/CANCELLED를 구분한다. 기존 정책에는 이 상태들을 클래스 참가자 화면의 이진 표시인 "결제완료/미결제"로 축약하는 규칙이 없다.

### User Story
- 관리자로서 모바일에서 클래스별 잔여석과 참가자의 예약·출결·결제 상태를 빠르게 확인하고 싶다.
- 관리자로서 만석이어도 운영상 필요하면 기존 예약 정책을 지키면서 특정 아이를 초과 예약하고 싶다.
- 관리자로서 클래스에서 아이를 고르거나 아이에서 클래스를 골라 같은 예약 절차로 등록하고 싶다.
- 관리자로서 정상 참가자와 취소 이력을 분리해 보되, 출결 후 취소된 예약의 실제 참석/불참 이력은 잃지 않고 확인하고 싶다.

### Scope
- 기본값 8인 클래스별 정원 관리와 기존 `ClassSchedule.capacity` 재사용
- 관리자 초과 예약 허용, 초과 예약도 일반 `Reservation`으로 저장
- 클래스 목록/운영 화면에서 예약 인원보다 잔여석을 우선 표시
- 클래스 상세의 참가자 통합 운영 화면: 아이, 예약 상태, 출결 상태, 단순 결제 여부, 취소 여부
- 정상 참가자와 취소 예약의 분리 표시
- 출결 후 CANCELLED 예약에 취소 상태와 보존된 PRESENT/ABSENT 이력을 함께 표시
- 클래스 상세→아이 선택→예약, 아이 상세→클래스 선택→예약의 양방향 진입점
- 오늘/이번 주/이번 달 KST 기간 프리셋과 진행 예정/종료 상태 필터
- 모바일에서 가로 스크롤 없이 핵심 상태와 주요 운영 액션을 파악할 수 있는 화면

### Out of Scope
- 대기예약 또는 대기 순번
- 자동 알림, 카카오톡, SMS
- 새로운 결제·환불 정책, 결제·환불 workflow 재설계, 자동 환불
- 예약 자동 취소
- 고객용 예약 페이지와 외부 사용자 self-service 예약
- 새로운 권한 체계
- Dashboard/Revenue 분석 기능 확장
- 쿠폰/프로모션 정책 엔진

### Business Rules
- 클래스 운영 기준 정원은 기존 `ClassSchedule.capacity`에 저장하고 기본값은 8명으로 유지한다. 새 정원 컬럼, migration, 기존 데이터 backfill은 필요하지 않다.
- capacity는 1~99명의 정수만 허용한다. 99명은 운영 권장치가 아니라 비정상 입력 방지를 위한 서버 validation 상한이며, 기존 DB의 `capacity >= 1` CHECK에는 상한을 추가하지 않는다(ADR-044).
- 관리자는 만석 클래스에도 초과 예약할 수 있다. 초과 예약은 별도 상태나 대기예약으로 만들지 않고 기존 `Reservation` 상태 모델을 그대로 사용한다(ADR-043).
- 초과 예약에도 동일 아이 중복 차단(ADR-024), 비활성 아이 차단(ADR-025), 취소·완료 클래스 차단(ADR-029)을 동일하게 적용한다.
- 진행 예정 클래스의 좌석 점유 인원은 RESERVED 예약만 포함한다. CANCELLED는 좌석을 점유하지 않으며, COMPLETED/NO_SHOW는 진행 예정 클래스의 잔여석 계산 대상이 아니다.
- 진행 예정 클래스는 `remainingSeats = capacity - RESERVED 예약 수`로 계산해 양수면 "잔여 N석", 0이면 "만석", 음수면 "정원 초과 N명"으로 표시한다. 종료 클래스는 좌석 문구 대신 "종료"를 표시한다.
- 정원 미만 예약은 기존 검증 통과 후 바로 허용한다. 정원 이상이면 관리자에게 초과 예약임을 경고하고 명시적 확인을 받아야 한다.
- 예약 생성의 ClassSchedule 행 잠금과 트랜잭션 내 최신 RESERVED 인원 재확인 방식은 유지한다(ADR-023). 서버는 확인 화면에서 보았던 인원이 아니라 잠금 후 최신 인원으로 초과 여부를 다시 판단해야 한다(ADR-044).
- 클래스 상세의 참가자 목록은 예약 상태를 가장 우선적인 상태로 표시한다.
- 정상 참가자 영역에는 RESERVED, COMPLETED, NO_SHOW 예약을 표시한다. 취소 예약 영역에는 Reservation.status=CANCELLED인 예약을 별도로 모은다.
- CANCELLED 중 attendance가 없는 건은 수업 전 일반 취소로 표시한다. attendance=PRESENT/ABSENT가 보존된 건은 출결 후 취소 이력으로 유지하고 각각 취소 상태와 참석/불참을 함께 표시한다(ADR-033, ADR-040, ADR-042).
- 출결 표시는 실제 attendance만 따른다. PRESENT는 "참석", ABSENT는 "불참", null은 "출결 미처리"이며, 결제·환불 상태만으로 attendance나 Reservation.status를 추론·변경하지 않는다. CANCELLED + PRESENT/ABSENT도 취소 상태와 이 출결 이력을 함께 표시한다.
- 참가자 통합 화면의 결제 정보는 빠른 확인용 "결제완료/미결제" 두 단계로만 표시한다. Payment.status가 PAID/PARTIAL_REFUNDED/REFUNDED이면 "결제완료", CANCELLED이거나 Payment가 없으면 "미결제"다. 이 요약은 해당 예약에 유효한 결제가 발생했는지만 뜻하며, 부분/전액 환불 상세와 환불 가능 여부는 기존 결제·환불 화면에서 확인한다(ADR-044).
- 예약 취소와 환불의 독립성 및 환불 자격은 ADR-035/ADR-039를 그대로 따른다. "결제완료/미결제" 요약은 표시 전용이며 Payment/Refund 상태, 환불 eligibility, 부분/전액 환불 workflow를 변경하지 않는다.
- 기존 클래스 상세에서 운영상 필요한 안전정보를 확인하는 기능은 유지하되, Phase 12 상태 요약을 이유로 안전정보나 개인정보를 클래스 목록·일반 예약 목록 등 더 넓은 화면에 새로 노출하지 않는다(ADR-006).
- 두 예약 진입점은 같은 예약 생성 화면, Zod 검증, Server Action, 동시성·중복·상태 검증을 재사용한다. 진입점별 별도 예약 정책을 만들지 않는다.
- 오늘/이번 주/이번 달은 KST 기준이며, 이번 주는 월요일~일요일이고 양 끝 날짜를 포함한다. 기간 프리셋과 상태 필터는 함께 적용할 수 있다. 기본 상태는 ADR-022대로 진행 예정이고 기본 날짜 프리셋은 없다.
- 진행 예정은 `status=SCHEDULED AND endsAt>=now`, 종료는 현재 "완료" 필터와 같은 `status=SCHEDULED AND endsAt<now` 기준을 사용한다(ADR-026, ADR-028). 기존 취소/전체 조회를 제거하지 않는다.

### Acceptance Criteria
- 클래스 생성 시 정원의 기본값이 8명이고, 관리자가 1~99명의 정수 범위에서 클래스별 정원을 변경하면 기존 `ClassSchedule.capacity`에 저장·표시된다. 0, 음수, 100 이상은 서버가 거부한다.
- 진행 예정 클래스의 RESERVED 예약만 좌석 점유에 포함되고 CANCELLED/COMPLETED/NO_SHOW는 잔여석 계산에서 제외된다.
- 클래스 목록/운영 화면은 `capacity - RESERVED 예약 수`에 따라 "잔여 N석", "만석", "정원 초과 N명"을 표시하며 예약 인원보다 잔여석을 우선한다. 종료 클래스에는 좌석 문구 대신 "종료"가 표시된다.
- 정원 미만에서는 기존 검증 통과 후 바로 예약되고, 정원 이상에서는 초과 예약 경고와 관리자의 명시적 확인 없이는 예약되지 않는다.
- 초과 확인 후 다른 예약이 들어와도 서버가 ClassSchedule 행을 잠그고 최신 RESERVED 인원으로 초과 여부를 다시 판단한다. 초과 예약에도 기존 중복·아이 활성·클래스 상태 검증이 적용된다.
- 클래스 상세 한 화면에서 각 아이의 예약 상태, 출결, 단순 결제 여부, 취소 여부를 확인할 수 있다.
- 일반 예약과 CANCELLED 예약이 구분되어 표시되고, 출결 후 CANCELLED 예약은 보존된 attendance에 따라 참석/불참 이력을 함께 보여준다.
- attendance=PRESENT/ABSENT/null은 각각 참석/불참/출결 미처리로 표시되고, Payment/Refund 상태를 바꿔도 출결 표시는 바뀌지 않는다.
- 참가자 통합 화면에서 PAID/PARTIAL_REFUNDED/REFUNDED는 "결제완료", CANCELLED 또는 Payment 없음은 "미결제"로 표시되며, 부분/전액 환불 상세와 환불 가능 여부는 기존 결제·환불 화면에만 유지된다.
- 클래스 상세에서 아이를 선택해 예약 화면으로 이동할 수 있고, 아이 상세에서도 클래스를 선택해 같은 예약 화면과 서버 검증으로 예약할 수 있다.
- 오늘/이번 주/이번 달 기간 프리셋과 진행 예정/종료 상태 필터가 기존 KST·상태 판정 규칙에 따라 동작하며, 기본 조회는 날짜 제한 없는 진행 예정 클래스다.
- 모바일에서 핵심 예약·출결·결제 상태를 가로 스크롤 없이 식별할 수 있고, 주요 운영 액션을 사용할 수 있다. 지나치게 많은 컬럼을 가진 데스크톱 전용 표를 모바일에 강제하지 않는다.
- 클래스 목록이나 일반 예약 목록에 안전정보·불필요한 개인정보가 추가로 노출되지 않는다.
- 대기예약, 새 결제/환불 정책, 자동 환불·취소, 고객용 예약, Dashboard/Revenue 확장이 포함되지 않는다.
- 상태 기반 그룹, 만석/초과 상태와 액션 노출, 양방향 예약 진입점은 Definition of Done에 따라 Playwright E2E로 검증한다.

### Conflicts
- 기존 1.3/8.3/10.4/18장과 Acceptance Criteria의 "최대 8명·8명 초과 예약 금지"는 Phase 12의 "기본 8명·capacity 상한 99·관리자 초과 예약 허용"과 충돌했으며 ADR-043/ADR-044 기준으로 정리했다.
- ADR-004의 DB `capacity>=1` 및 기본값 8은 유지한다. 서버 상한 8은 application validation 상한 99로 대체하며 DB CHECK에 상한을 추가하지 않는다(ADR-044).
- ADR-023의 행 잠금과 RESERVED COUNT는 그대로 유지한다. 이전에는 capacity 도달 시 하드 차단에 사용했지만, Phase 12에서는 최신 좌석 상태를 재확인하고 명시적 초과 예약 여부를 판정하는 데 사용한다.
- "결제 완료 후 환불 불가 상태는 불참으로 표시"는 결제 상태로 출결을 추론하는 의미가 아니다. 실제 attendance=ABSENT인 예약을 결제·환불 상태와 관계없이 "불참"으로 표시하는 것으로 확정해 ADR-031/ADR-033/ADR-039와의 충돌을 해소했다.

### Open Questions
없음. 기존 5건은 ADR-044와 위 Business Rules로 모두 해소되었다. 클래스 목록의 기본 상태는 ADR-022에 따라 진행 예정(SCHEDULED)이며 날짜 프리셋은 사용자가 선택하기 전에는 적용하지 않는다.

### Priority
Now — 실제 관리자 사용에서 모바일 클래스 운영과 참가자·결제 확인의 불편이 확인되었고, Phase 12 Open Questions가 모두 해소되어 설계 단계로 진행할 수 있다.

## Phase 13 — 아이 통합 이력

### Problem
- 아이 상세에는 기본·안전·동의 정보와 별도의 예약 이력, 결제·환불 이력이 함께 있지만 예약별 클래스·출결·취소·결제 상태를 한 흐름에서 비교하기 어렵다.
- 현재 아이 예약 이력은 전체 행을 제한 없이 가져오고 attendance와 Payment 상태를 함께 조회하지 않으며, 결제·환불 이력은 별도 쿼리와 금액 중심 섹션으로 분리되어 있다.
- 관리자는 한 아이의 예정 수업과 지난 운영 결과를 모바일에서 빠르게 파악하기 위해 여러 화면을 오가야 한다.

### Current Process
- 아이 상세 route는 `/children/[id]`이며 기본 정보, 보호자 연락처, 운영 메모, 안전정보, 동의 현황, 예약 이력, 결제·환불 이력과 기존 운영 액션을 제공한다.
- `listReservationsByChild`는 클래스 시작일 내림차순으로 해당 아이의 모든 예약을 한 번에 조회하지만 attendance와 Payment 상태는 포함하지 않고 pagination도 없다.
- `listPaymentItemsByChild`는 같은 아이의 결제 항목과 Refund 상세·금액을 별도 조회한다. 예약별 통합 상태를 만들기 위한 조회가 아니다.
- 기존 예약 상세 `/reservations/[id]`, 클래스 상세 `/classes/[id]`, 아이 기준 예약 진입 `/reservations/new?childId=<id>`가 모두 존재한다.
- Reservation에는 attendance가 직접 저장되고, 예약당 선택적 PaymentItem 1건이 Payment에 연결된다. 요구한 통합 이력은 현재 schema의 관계만으로 구성할 수 있다.
- 목록 pagination은 `page` 기반 20건 패턴이 있으나 상세 화면 안의 "더보기" 패턴은 아직 없다.

### User Story
- 관리자로서 아이 상세 상단에서 총 예약·참석·불참·취소·예정 규모를 한눈에 확인하고 싶다.
- 관리자로서 앞으로 갈 클래스와 최근 참여·노쇼·취소 이력을 분리해 보면서 예약·출결·결제 상태를 함께 확인하고 싶다.
- 관리자로서 모바일에서도 필요한 이력을 빠르게 훑고 기존 클래스/예약 상세와 새 예약 화면으로 이동하고 싶다.

### Scope
- 아이 상세 상단의 총 예약/참석/불참/취소/예정 5개 요약 지표
- 예정된 클래스와 지난 이력의 분리 표시
- Reservation, attendance, ClassSchedule, Payment 상태의 독립 표시
- CANCELLED + PRESENT/ABSENT/null 이력 보존 표현
- 아이 이력용 결제 상태 상세 표시: 결제완료/부분환불/전액환불/미결제
- 지난 이력 최근 10건 초기 표시와 중복·누락 없는 더보기
- 기존 클래스 상세·예약 상세 이동
- 기존 `childId` prefill을 이용한 새 예약 진입
- 모바일 우선 배치, N+1 금지, 최소 개인정보 조회

### Out of Scope
- Customer/Guardian 도메인 또는 보호자 CRM 신설
- 가구·가족 단위 통합 화면, 고객 등급·태그·CRM 메모
- 고객별 총매출, LTV, 재방문율, 세그먼트 등 분석
- 결제금액·환불금액·환불 가능 금액의 아이 통합 이력 표시
- Payment/Refund 상태 머신, 환불 eligibility, 예약 취소·환불 독립 정책 변경
- 쿠폰·포인트·프로모션, 문자/SMS·카카오톡·자동 알림
- 고객용 화면, self-service 예약, 데이터 export
- Dashboard/Revenue 확장 또는 새로운 출결 정책
- 신규 model/field/enum, migration, backfill

### Business Rules
- 총 예약은 Child에 연결된 Reservation 전체 행 수이며 RESERVED/COMPLETED/NO_SHOW/CANCELLED를 모두 포함한다.
- 참석과 불참은 최종 Reservation.status가 아니라 보존된 attendance 원본을 기준으로 각각 PRESENT와 ABSENT를 센다. 출결 후 CANCELLED된 예약도 포함한다.
- 취소는 attendance와 무관하게 현재 Reservation.status=CANCELLED인 행을 센다.
- 예정은 Reservation.status=RESERVED, ClassSchedule.status=SCHEDULED, ClassSchedule.endsAt>=now를 모두 만족하는 예약이다. 기존 endsAt 기반 조회 시점 판정(ADR-026/ADR-028)을 사용하며 과거 RESERVED stale 행은 예정에서 제외한다.
- 요약 지표는 서로 다른 상태축의 집계이므로 상호 배타적이지 않다.
- 예정 영역은 가까운 클래스부터 표시한다. 지난 이력은 최근 클래스부터 표시하고 CANCELLED 예약은 클래스 날짜와 무관하게 예정 영역에서 제외한다.
- ClassSchedule.status=CANCELLED이면서 Reservation.status=RESERVED인 예약은 클래스 날짜가 미래여도 예정 영역에서 제외하고 즉시 지난 이력에 포함한다. 별도 취소 수업 영역을 만들지 않으며, "수업 취소"와 Reservation의 원본 RESERVED 상태를 독립적으로 표시한다(ADR-046).
- Reservation, attendance, ClassSchedule, Payment는 독립적인 원본 상태축으로 표시하며 조회를 위해 새 DB status를 만들거나 원본 상태를 변경하지 않는다.
- CANCELLED+attendance null/PRESENT/ABSENT는 각각 취소·출결 미처리, 취소됨·참석, 취소됨·불참 이력을 확인할 수 있어야 한다(ADR-033/ADR-044).
- 아이 통합 이력의 Payment 표시는 PAID=결제완료, PARTIAL_REFUNDED=부분환불, REFUNDED=전액환불, CANCELLED 또는 Payment 없음=미결제다. Phase 12 클래스 참가자 화면의 "결제완료/미결제" 이진 요약은 그대로 유지한다.
- 지난 이력은 최근 10건만 처음 표시하고 더보기로 이전 이력을 중복·누락 없이 이어서 조회한다. pagination 방식과 추가 batch 크기, 동률 정렬 기준은 PLAN의 기술 설계로 정한다.
- 기존 아이 상세의 기본 정보, 보호자 연락처, 운영 메모, 안전정보, 동의 및 운영 액션을 유지하되 보호자 연락처를 별도 요약으로 중복 강조하지 않는다.
- 새 예약은 `/reservations/new?childId=<id>`와 기존 폼·검증·Server Action을 재사용한다. 클래스/예약 이동도 기존 상세 route를 사용한다.
- 통합 이력 조회는 safetyInfo, Refund 상세·금액, 불필요한 개인정보를 포함하지 않고 N+1을 만들지 않는다.
- 클래스 정원·초과 예약·중복/비활성/종료 차단, 출결 전환, 취소 후 attendance 보존, 취소·환불 독립, Payment/Refund lifecycle, Dashboard/Revenue 집계, KST, 권한과 안전정보 경계는 변경하지 않는다.

### Acceptance Criteria
- 아이 상세에서 총 예약/참석/불참/취소/예정 5개 지표를 확인할 수 있고 각 값이 위 Business Rules와 일치한다.
- 총 예약에는 CANCELLED가 포함되며, 참석/불참은 CANCELLED 여부와 무관하게 attendance 원본으로 계산된다.
- 예정된 클래스와 지난 이력이 분리되고, 예정은 가까운 순서, 지난 이력은 최근 클래스 순서로 표시된다.
- 미래 ClassSchedule이 CANCELLED이고 Reservation은 RESERVED로 남아 있어도 예정에서는 제외되고 지난 이력에 포함되며, 수업 취소와 예약 취소가 구분되어 표시된다.
- CANCELLED+PRESENT/ABSENT/null에서 취소 상태와 보존된 출결 상태를 함께 확인할 수 있다.
- 각 이력에서 예약 상태, attendance, 클래스/프로그램과 Payment 상태를 서로 독립적으로 확인한다.
- PAID/PARTIAL_REFUNDED/REFUNDED/CANCELLED/Payment 없음이 각각 결제완료/부분환불/전액환불/미결제로 표시된다.
- 아이 통합 이력에는 결제금액, 환불금액, 환불 가능 금액이 표시되지 않는다.
- 지난 이력은 최근 10건만 먼저 표시되고, 더보기 후 이전 이력이 중복·누락 없이 이어진다.
- 각 이력에서 기존 클래스 상세와 예약 상세로 이동할 수 있다.
- 아이 상세의 예약 추가는 기존 `childId` prefill 흐름과 동일한 서버 검증을 사용한다.
- 모바일에서 요약과 예정/지난 이력의 핵심 상태·이동을 가로 스크롤 없이 사용할 수 있다.
- 통합 이력 조회에 N+1이 없고, safetyInfo·Refund 상세/금액·불필요한 개인정보를 조회하거나 일반 목록에 노출하지 않는다.
- 기존 예약·출결·취소·결제·환불·Dashboard·Revenue 정책과 상태 전환에 회귀가 없다.
- 신규 Prisma model/field/enum, migration, backfill이 없다.

### Conflicts
- Phase 12 참가자 화면은 PAID/PARTIAL_REFUNDED/REFUNDED를 모두 "결제완료"로 축약하지만, Phase 13 아이 이력은 부분/전액 환불 여부까지 구분한다. 이는 같은 원본 Payment.status를 목적별로 다르게 표시하는 것이며 ADR-044의 클래스 운영용 이진 요약을 대체하지 않는다(ADR-045).
- 클래스 취소는 연결된 Reservation을 자동 취소하지 않는다(9.4). 미래 CANCELLED 클래스에 `Reservation.status=RESERVED`가 남는 경우도 예약 상태를 바꾸지 않고 지난 이력에 포함하며 "수업 취소"로 구분하기로 해 충돌을 해소했다(ADR-046).
- 현재 아이 상세의 별도 예약 이력과 결제·환불 금액 중심 이력은 Phase 13의 상태 중심 통합 이력으로 재구성하며, 아이 상세 이력에서는 금액을 제거한다. 기존 결제·환불 관리 route와 정책은 제거하지 않는다.

### Open Questions
없음. 미래 ClassSchedule이 CANCELLED이고 Reservation이 RESERVED로 남은 경우도 즉시 지난 이력에 포함하기로 확정해 마지막 분류 질문을 해소했다(ADR-046).

### Priority
Now — Phase 12까지 운영 데이터와 상태축이 갖춰졌고, 관리자가 아이 한 명의 예정 수업과 과거 운영 이력을 모바일에서 통합 확인해야 하는 실사용 요구가 다음 우선순위다. Open Questions가 모두 해소되어 PLAN으로 진행할 수 있다.

## Phase 14 — 운영 안정화 2차

### Problem
- Phase 13 아이 통합 이력에서 `ClassSchedule.status=CANCELLED`, `Reservation.status=RESERVED`인 예약에 "예약 취소" 액션이 표시되지만, 최종 Server Action은 RESERVED 예약의 소속 클래스가 SCHEDULED이고 종료되지 않은 경우에만 쓰기를 허용하므로 이 액션은 항상 실패한다.
- 아이 상세의 `historyPage`는 정수 overflow만 피하는 매우 큰 상한을 사용한다. URL을 직접 조작하면 아이 한 명의 이력 범위라도 비정상적으로 큰 누적 조회량이 Prisma `take`로 전달될 수 있다.

### Current Process
- 공통 `canCancelReservation()`은 아이 통합 이력, 클래스 참가자 화면, 예약 상세, 예약 취소 직접 URL, Server Action 사전검증에서 재사용된다. 현재 RESERVED 예약은 클래스의 시간상 종료 여부만 반영하고 ClassSchedule의 CANCELLED 상태를 취소 불가로 판정하지 못한다.
- 예약 취소 Server Action의 최종 조건부 갱신은 RESERVED 예약에 `ClassSchedule.status=SCHEDULED`와 `ClassSchedule.endsAt>=취소 시각`을 요구한다. 따라서 잘못 노출된 UI와 직접 URL form을 거쳐 제출해도 DB 쓰기는 fail-close되지만, 사용자에게 실행 불가능한 액션이 제공된다.
- 아이 지난 이력은 `CHILD_HISTORY_PAGE_SIZE=10`이고 `take=effective page*10`으로 누적 조회한다. 현재 `MAX_CHILD_HISTORY_PAGE`는 32비트 정수 overflow 방지 수준이어서 실질적인 요청량 제한 역할을 하지 못한다.

### User Story
- 관리자로서 화면에 표시된 예약 취소 액션이 실제 서버에서 허용되는 조건과 일치하여, 항상 실패하는 취소 흐름에 진입하지 않고 싶다.
- 관리자로서 잘못되거나 과도한 `historyPage` URL이 들어와도 아이 이력 조회량이 안전한 범위로 정규화되기를 원한다.

### Scope
- RESERVED 예약의 UI 취소 가능 조건을 실제 Server Action의 쓰기 조건과 일치시킨다.
- 취소된 클래스의 RESERVED 예약에서 아이 이력·클래스 참가자·예약 상세의 예약 취소 액션을 숨긴다.
- 같은 예약의 `/reservations/[id]/cancel` 직접 접근에서 취소 가능한 form을 제공하지 않는다.
- Server Action의 기존 fail-close 검증을 유지하고 관련 직접 제출 회귀를 검증한다.
- 아이 이력의 페이지 크기 10을 유지하면서 `MAX_CHILD_HISTORY_PAGE=100`과 최대 누적 `take=1000`을 적용한다.
- `historyPage`의 기본값·잘못된 입력·상한 초과 입력을 정규화하고 기존 더보기 동작을 유지한다.
- Phase 13 및 기존 예약 취소 흐름의 unit/E2E 회귀 검증을 보강한다.

### Out of Scope
- 새로운 Reservation/ClassSchedule status, 자동 예약 취소, 자동 환불 또는 Refund 생성
- Payment/Refund/Attendance 정책이나 Phase 13 summary·upcoming·past predicate 변경
- 아이 통합 이력 재설계, cursor pagination 전환, pagination redesign
- DB index, Prisma schema/migration, 새로운 model/field/enum
- Dashboard/Revenue, CRM, 알림, 고객 화면 및 unrelated UI polish

### Business Rules
- `Reservation.status=RESERVED`인 예약은 `ClassSchedule.status=SCHEDULED`이고 `ClassSchedule.endsAt>=now`일 때만 취소 가능한 것으로 UI와 직접 취소 URL에서 판단한다.
- `ClassSchedule.status=CANCELLED`, `Reservation.status=RESERVED`인 경우 "수업 취소"와 원본 "예약됨" 이력을 그대로 표시하되 예약 취소 버튼/링크를 노출하지 않고 직접 취소 URL에서도 취소 form을 제공하지 않는다.
- 클래스 취소를 이유로 Reservation을 자동 CANCELLED로 바꾸거나 attendance/Payment를 변경하거나 Refund를 만들지 않는다. 자동 환불도 하지 않는다.
- 정상 SCHEDULED+RESERVED 예약의 기존 취소 흐름, COMPLETED/NO_SHOW의 사후 취소 및 attendance 보존(ADR-033), 출결 없이 종료된 RESERVED 예약의 취소 차단(ADR-027)은 그대로 유지한다.
- Server Action은 UI 판정을 신뢰하지 않고 기존 조건부 갱신으로 클래스 상태·종료 시각·예약 상태를 다시 확인한다. UI에서 액션을 숨겨도 직접 제출에 대한 fail-close 검증을 제거하거나 약화하지 않는다.
- `CHILD_HISTORY_PAGE_SIZE`는 10, `MAX_CHILD_HISTORY_PAGE`는 100이며 한 요청의 누적 history query `take`는 1000을 초과할 수 없다.
- `historyPage`가 없거나 빈 값, 정수 형식이 아닌 문자열, 0 또는 음수이면 effective page는 1이다. 1~100 정수는 그대로 사용하고 100을 초과하는 정수는 100으로 clamp한다.
- 기존 누적 더보기는 page 1→10건, page 2→20건, page 3→30건 방식으로 유지하며 page 100에서 최대 1000건까지 조회한다.
- 최신 이력 우선 정렬, 결정적 tie-break, count/hasMore, URL source of truth, 새로고침·뒤로가기, `#past-history` anchor의 의미를 변경하지 않는다.
- Phase 13의 예정/지난 이력 분류와 ADR-046은 변경하지 않는다. 취소된 클래스의 RESERVED 예약은 계속 예정에서 제외되고 지난 이력에 포함된다.

### Acceptance Criteria
- ClassSchedule=SCHEDULED, Reservation=RESERVED이고 `endsAt>=now`인 예약에는 기존 예약 취소 액션이 표시되고 정상 취소할 수 있다.
- ClassSchedule=CANCELLED, Reservation=RESERVED인 아이 이력에는 "수업 취소"와 "예약됨"이 표시되지만 예약 취소 액션은 표시되지 않는다.
- 위 예약의 `/reservations/[id]/cancel` 직접 URL에는 취소 가능한 form이 표시되지 않는다.
- 위 예약의 Server Action을 직접 제출해도 기존 fail-close 검증이 요청을 거부하고 Reservation, attendance, Payment, Refund 데이터가 변경되지 않는다.
- COMPLETED/NO_SHOW의 기존 사후 취소, attendance 보존, 정상 RESERVED 취소 및 출결 없는 종료 클래스 RESERVED 취소 차단이 그대로 동작한다.
- 수업 취소와 예약 취소의 의미가 독립적으로 유지되고 ADR-046의 예정 제외·지난 이력 포함·취소 summary 미증가 규칙에 회귀가 없다.
- `historyPage` 없음, 빈 값, invalid string, 0, 음수는 1로 정규화된다.
- `historyPage` 1, 2, 99, 100은 각각 같은 effective page가 된다.
- `historyPage` 101과 매우 큰 정수는 100으로 clamp된다.
- history query의 누적 `take`는 어떤 URL 입력에서도 1000을 초과하지 않는다.
- 기존 10→20→30 누적 더보기, 중복·누락 없는 정렬, URL·reload·back·anchor 동작이 유지된다.
- 취소 가능 여부와 `historyPage` 경계값을 unit test로 검증하고, 취소된 클래스 RESERVED 예약의 액션 미노출·직접 URL·Server Action fail-close 및 Phase 13 이력 분류를 E2E/관련 회귀 테스트로 검증한다.
- 기존 Phase 13 unit/E2E, 예약 취소 흐름, 전체 unit/lint/typecheck/build가 통과한다.
- Prisma schema/migration과 기존 결제·환불·출결·Dashboard·Revenue 정책에 변경이 없다.

### Conflicts
- 없음. Issue A는 ADR-027의 서버 차단, ADR-033의 출결 후 취소 예외, ADR-046의 수업 취소/예약 취소 독립 표시를 바꾸지 않고 공통 UI eligibility를 최종 쓰기 조건과 맞추는 보강이다.
- Issue B는 비정상 URL 입력을 제한하는 technical safety cap이며 기존 Phase 13의 10건 단위 누적 더보기 정책과 충돌하지 않는다.

### Open Questions
없음. 공통 취소 eligibility 사용처와 직접 취소 페이지가 확인되었고, 최대 page 100 clamp는 기존 pagination 비즈니스 정책과 충돌하지 않는다.

### Priority
Now — Phase 13 QA/Review에서 확인된 두 non-blocking MINOR를 다음 기능 확장 전에 제거해 UI와 서버 규칙의 정합성 및 조회 입력 안전성을 높이는 작은 안정화 작업이다.

## Phase 15 — 클래스 반복 등록

### Problem
- 현재 클래스 생성은 날짜 1건씩만 등록할 수 있다.
- 같은 프로그램을 같은 요일·시간으로 한 달 동안 반복 운영할 때 프로그램, 시간, 장소, 정원, 선생님, 메모와 보험 정보를 날짜마다 다시 입력해야 한다.
- 반복되는 입력은 운영 시간을 늘리고 날짜 누락이나 대량 오등록 위험을 만든다.

### Current Process
- 관리자는 `/classes/new`에서 활성 프로그램과 활성 선생님, 날짜 1개, 시간, 장소, 정원, 메모와 보험 정보를 입력해 `ClassSchedule` 1건을 생성한다.
- 서버는 관리자 권한, 입력값, 프로그램·선생님 활성 상태와 기존 단건 클래스 규칙을 검증한 뒤 클래스와 선생님 배정을 저장한다.
- 같은 내용의 다음 날짜를 만들려면 생성 화면으로 돌아가 모든 값을 다시 입력해야 한다.
- 기존 단건 등록은 과거 날짜를 별도로 차단하지 않으며, 같은 시간의 기존 클래스나 선생님 일정 겹침을 검사하지 않는다.

### User Story
- 관리자로서 월 단위로 반복 운영하는 클래스의 공통 정보를 한 번만 입력하고, 같은 달의 선택한 요일에 해당하는 클래스들을 안전하게 한 번에 등록하고 싶다.
- 관리자로서 저장 전에 생성될 날짜와 개수를 확인하고, 일부만 생성되는 결과 없이 요청 전체의 성공 또는 실패를 명확하게 알고 싶다.

### Scope
- 기존 `/classes/new`에서 단건 등록과 반복 등록 모드 선택
- 같은 달 안의 시작일·종료일과 복수 요일을 이용한 반복 날짜 지정
- 시작일·종료일을 포함한 KST 날짜 기준
- 프로그램, 시간, 장소, 정원, 선생님, 메모와 보험 정보의 공통 입력
- 생성될 날짜 목록, 총 클래스 수와 반복 조건의 필수 미리보기
- 반복 대상의 과거 날짜, 기존 클래스 중복과 기존 단건 validation 검증
- 하나라도 실패하면 아무 클래스도 만들지 않는 all-or-nothing 생성
- 독립적인 여러 `ClassSchedule` 생성과 기존 클래스별 후속 운영
- 반복 조건과 미리보기를 모바일에서 가로 스크롤 없이 확인할 수 있는 운영 UX

### Out of Scope
- 특정 날짜 직접 복수 선택
- 여러 달을 한 요청으로 일괄 등록
- 날짜별 시간·장소·정원·선생님·메모·보험 정보 개별 수정
- 선생님 일정 겹침 차단 또는 별도 scheduling engine
- recurrence/series/group model과 recurrence exception
- 반복 전체·이후 일정 일괄 수정 또는 일괄 취소
- 기존 클래스의 series 편입
- Google Calendar 수준 recurrence 또는 RRULE 저장
- 예약·출결·결제·환불 복제나 자동 처리
- 반복 예약, 고객용 예약, SMS/Kakao 알림, calendar drag/drop
- ClassSchedule COMPLETED 전환 메커니즘

### Business Rules
- 반복 등록은 시작일과 종료일이 같은 달에 속할 때만 허용한다. 두 날짜를 모두 포함하고 선택한 하나 이상의 요일에 해당하는 날짜를 생성 대상으로 삼는다.
- 반복 등록으로 달라지는 값은 날짜뿐이다. 프로그램, 시작·종료 시간, 장소, 정원, 선생님, 메모, 보험 여부, 보험사, 증권번호와 안전 메모는 모든 대상에 동일하게 적용한다.
- 기존 직전 클래스 보험정보 불러오기를 사용할 수 있으며, 최종 입력된 보험 값을 모든 생성 클래스에 동일하게 적용한다.
- 실제 생성 전에 반복 조건, 생성될 날짜 목록과 총 생성 클래스 수를 반드시 확인할 수 있어야 한다.
- 반복 대상에 현재 KST 기준 과거 날짜가 하나라도 있으면 전체 요청을 거부한다. 기존 단건 등록의 과거 날짜 허용 동작은 변경하지 않는다.
- 기존 ClassSchedule과 프로그램, `startsAt`, `endsAt`, 장소가 모두 같은 대상이 하나라도 있으면 중복으로 판단해 전체 요청을 거부한다. 중복 날짜만 제외하거나 나머지만 생성하지 않는다.
- 선생님의 다른 클래스와 시간대가 겹치는지는 Phase 15에서 새로 차단하지 않는다. 한 클래스 안에서 동일 선생님을 중복 배정하지 않는 기존 규칙은 유지한다.
- 모든 대상은 기존 단건 클래스의 관리자 권한, 유효한 날짜·시간, `startsAt<endsAt`, 장소, 정원 1~99, 활성 Program, 활성 Teacher, 선생님 최소 1명과 동일 선생님 중복 금지 규칙을 각각 만족해야 한다. 선생님 2명 원칙과 2명 미만 경고도 유지한다.
- 대상 중 하나라도 validation, 프로그램·선생님 활성 상태, 과거 날짜, 중복 또는 기존 business rule을 위반하면 어떤 클래스와 선생님 배정도 생성하지 않는다. 부분 성공은 허용하지 않는다.
- 반복 등록으로 생성된 각 결과는 서로 독립적인 `ClassSchedule`이며 별도 반복 관계를 저장하지 않는다. 이후 수정·취소·예약·출결·결제·환불은 클래스별로 기존 정책을 적용한다.
- 반복 등록은 예약·출결·결제·환불 상태나 정책을 변경하거나 관련 데이터를 자동 생성하지 않는다.
- 기존 단건 등록 workflow와 동작은 유지한다(docs/DECISIONS.md ADR-047).

### Acceptance Criteria
- 관리자는 기존 `/classes/new`에서 단건 등록 또는 반복 등록을 선택할 수 있고, 단건 등록의 기존 입력·검증·저장 동작에 회귀가 없다.
- 반복 등록에서 같은 달의 시작일·종료일과 하나 이상의 요일을 선택할 수 있으며, 복수 요일을 함께 선택할 수 있다.
- 시작일과 종료일이 생성 대상 요일이면 해당 날짜를 포함하고, 서로 다른 달의 시작일·종료일은 거부한다.
- 선택 기간과 요일에 해당하는 날짜만 생성 대상이 되며 특정 날짜 직접 복수 선택 기능은 제공하지 않는다.
- 저장 전에 반복 조건, 생성될 모든 날짜와 총 생성 클래스 수가 표시되고 운영자가 이를 확인할 수 있다.
- 프로그램, 시작·종료 시간, 장소, 정원, 선생님, 메모와 보험 정보가 모든 생성 클래스에 동일하게 저장된다.
- 반복 대상 중 현재 KST 기준 과거 날짜가 하나라도 있으면 아무 클래스도 생성되지 않으며, 기존 단건 등록의 과거 날짜 동작은 유지된다.
- 프로그램, `startsAt`, `endsAt`, 장소가 모두 같은 기존 클래스가 하나라도 있으면 아무 클래스도 생성되지 않는다.
- 선생님의 다른 클래스 시간과 겹친다는 이유만으로 반복 등록을 차단하지 않으며, 동일 클래스 내 선생님 중복은 기존 규칙대로 거부한다.
- 대상 중 하나의 입력이나 business rule만 실패해도 ClassSchedule과 선생님 배정을 포함한 요청 전체가 생성되지 않는다.
- 성공하면 미리보기의 날짜 수와 실제 생성된 독립 ClassSchedule 수가 같고, 각 클래스는 기존 상세·수정·취소·예약·출결·결제·환불 흐름을 개별적으로 사용한다.
- 반복 등록 결과에 recurrence/series/group 관계나 새 상태가 저장되지 않는다.
- 모바일에서 반복 조건, 생성 날짜 목록, 총 개수와 최종 생성 동작을 가로 스크롤 없이 확인할 수 있다.
- 기존 예약·출결·결제·환불·정원·클래스 상태·KST·권한·보험 정책에 변경이 없다.
- 기존 모델만 사용하며 신규 Prisma model/field/enum, migration 또는 backfill이 없다.

### Conflicts
- 기존 `ClassSchedule`은 실제 클래스 1건을 나타낸다. 반복 등록 결과를 독립 row로 생성하므로 이 정의와 충돌하지 않으며 별도 series model이 필요하지 않다.
- 기존 단건 등록은 과거 날짜를 허용하지만 반복 등록은 대량 오등록 방지를 위해 과거 날짜가 하나라도 있으면 전체 거부한다. 이는 단건 정책 변경이 아니라 반복 등록에만 적용하는 별도 안전 규칙이다.
- 현재 시스템에는 클래스 중복 또는 선생님 일정 충돌 차단 규칙이 없다. Phase 15는 프로그램·시작/종료 시각·장소가 모두 같은 기존 클래스만 중복으로 차단하고 선생님 일정 충돌 정책은 새로 만들지 않는다.
- ADR-032의 보험 계약 형태 미결정은 그대로 유지한다. Phase 15는 기존 보험 입력과 직전 값 불러오기를 재사용해 확정된 입력값을 각 클래스에 복사할 뿐 보험 계약 정책을 추가하지 않는다.
- 반복 등록의 all-or-nothing 결과는 기존 단건 validation을 각 대상에 동일하게 적용하며 예약·결제·환불의 원자성이나 상태 정책을 변경하지 않는다.

### Open Questions
없음. 반복 날짜, 단건/반복 관계, 공통 입력, 필수 미리보기와 한 달 범위, 과거 날짜, 중복, 전체 실패와 독립 ClassSchedule 정책을 사용자가 확정했다(ADR-047).

### Priority
Now — 월 단위 반복 클래스의 동일 정보 재입력과 날짜 누락 위험을 줄이는 실사용 요구이며, 범용 recurrence 시스템 없이 기존 ClassSchedule 구조로 해결할 수 있다.

## Phase 16 — 계정·권한 관리

### Problem
- 현재 `User`에는 ADMIN과 TEACHER 역할이 존재하지만 운영 화면과 Server Action은 사실상 ADMIN만 사용할 수 있다.
- 로그인 계정인 User와 수업 배정 데이터인 Teacher가 연결되어 있지 않아 로그인한 선생님이 어떤 클래스의 운영자인지 안전하게 판정할 수 없다.
- 계정 비활성화나 역할 변경 후에도 기존 JWT의 과거 권한이 남을 수 있고, 계정 생성·비밀번호 재설정·관리자 보호·계정 변경 이력 기능이 없다.
- 현재 seed는 기존 ADMIN의 password, role, isActive를 덮어쓸 수 있어 계정 관리 도입 후 운영 계정을 손상시킬 위험이 있다.

### Current Process
- 내부 운영자는 이메일과 비밀번호의 credentials 방식으로 로그인한다. 로그인 시 활성 User와 비밀번호 hash를 확인하고 역할을 JWT session에 담는다.
- 보호된 관리자 layout과 주요 server mutation은 `requireAdmin()`을 사용하므로 TEACHER 역할 User는 로그인하더라도 운영 기능을 사용할 수 없다.
- 대부분의 server query는 역할별 데이터 범위를 직접 판정하지 않고 ADMIN 전용 화면 경계에 의존한다.
- User와 Teacher는 별도 entity이며 FK나 1:1 관계가 없다.
- 계정 목록·생성·수정·비활성화·비밀번호 변경/재설정 UI와 최소 계정 변경 이력이 없다.

### User Story
- 관리자로서 내부 운영 계정을 만들고 역할과 활성 상태를 안전하게 관리하며, 최소 한 명의 활성 관리자를 항상 유지하고 싶다.
- 선생님으로서 내 계정으로 로그인해 내가 배정된 클래스의 운영 정보와 현장 안전정보를 확인하고 출결을 기록하고 싶다.
- 관리자로서 계정이나 선생님 상태와 역할을 변경하면 기존 로그인 세션에도 다음 보호된 요청부터 최신 권한이 적용되기를 원한다.
- 운영 책임자로서 계정 생성과 주요 변경의 처리자·시각을 추적하되 비밀번호나 session 같은 비밀정보는 기록하지 않기를 원한다.

### Scope
- ADMIN/TEACHER 두 역할을 이용한 내부 운영자 계정 관리
- ADMIN 전용 계정 목록, 생성, 이름·이메일·역할·활성 상태 변경, 비밀번호 재설정
- TEACHER User와 기존 활성 Teacher의 필수 1:1 연결
- 최초 로그인 및 비밀번호 재설정 후 강제 비밀번호 변경
- 변경 완료 후 다음 보호된 서버 요청부터 최신 계정·역할·Teacher 활성 상태 적용
- ADMIN 전체 운영 권한 유지
- TEACHER의 본인 배정 클래스 운영 Dashboard·클래스·참가자 조회와 출결 기록
- 배정 클래스 참가자에 한정한 아이 이름, 보호자 연락처, ChildSafetyInfo 조회
- TEACHER 본인 Teacher 정보와 같은 클래스 공동 배정 Teacher 이름 조회
- Navigation, 직접 URL, server query, Server Action의 역할·배정 범위 강제
- 마지막 활성 ADMIN과 본인 계정 보호
- 계정 생성·이메일·역할·활성 상태·비밀번호 재설정의 append-only 최소 변경 이력
- 기존 ADMIN 계정과 인증 상태를 보존하고 운영 계정을 덮어쓰지 않는 seed 동작
- 역할·배정·민감정보 경계와 계정 lifecycle의 unit/E2E 검증

### Out of Scope
- 공개 회원가입과 고객용 계정
- 이메일 초대·메일 발송·이메일 인증
- Google/Kakao 등 소셜 로그인과 OAuth
- MFA, SSO, 조직/tenant
- OWNER/MANAGER/STAFF 등 추가 역할
- 사용자별 custom permission과 role permission editor
- 선생님용 별도 모바일 앱
- TEACHER의 전체 아이 목록·전체 아이 상세, 아이 등록·수정
- TEACHER의 ChildSafetyInfo 수정과 동의 조회·변경 관리
- TEACHER의 클래스 생성·반복 생성·수정·취소 및 예약 생성·취소
- TEACHER의 결제·금액·환불·Revenue·재무 Dashboard·전체 Teacher 관리
- User 물리 삭제
- 전체 시스템 범용 AuditLog
- 기존 예약·출결·결제·환불·Revenue 정책 변경

### Business Rules
- 역할은 `ADMIN`, `TEACHER` 두 개만 유지한다.
- TEACHER User는 기존 활성 Teacher 한 명과 필수 1:1로 연결한다. 한 Teacher에 여러 User를 연결하거나 한 TEACHER User를 여러 Teacher에 연결할 수 없으며 비활성 Teacher와 신규 TEACHER 계정을 연결할 수 없다. ADMIN은 Teacher 연결 없이 존재할 수 있다.
- TEACHER 접근은 User와 연결 Teacher가 모두 활성일 때만 허용한다. 어느 한쪽이 비활성화되면 변경 완료 후 다음 보호된 서버 요청부터 기존 session도 운영 기능에 접근할 수 없다.
- ADMIN은 기존 전체 운영 권한을 유지한다. TEACHER는 `ClassTeacher` 기준 본인 배정 ClassSchedule만 조회하고 해당 클래스 운영에 필요한 Dashboard만 볼 수 있다.
- TEACHER는 본인 배정 클래스 참가자의 아이 이름, 보호자 연락처와 ChildSafetyInfo만 조회할 수 있다. 전체 아이 목록·전체 아이 상세·아이 등록/수정·ChildSafetyInfo 수정·동의 관리에는 접근할 수 없다.
- TEACHER가 수행할 수 있는 운영 쓰기는 본인 배정 클래스의 출결 기록뿐이다. 클래스 생성·반복 생성·수정·취소와 예약 생성·취소는 허용하지 않는다.
- 출결을 포함한 모든 TEACHER 권한은 서버가 현재 User→Teacher 연결과 `ClassTeacher` 배정을 검증한다. 클라이언트의 userId, teacherId, role은 신뢰하지 않는다.
- TEACHER는 결제·결제 금액·환불·Revenue·재무 Dashboard·전체 Teacher 관리 화면을 볼 수 없다. 본인 Teacher 정보와 같은 배정 클래스의 공동 Teacher 이름은 볼 수 있다.
- ADMIN만 계정을 생성한다. 생성 시 ADMIN이 기존 비밀번호 validation을 만족하는 임시 비밀번호를 지정하며, 비밀번호는 hash만 저장하고 기존 비밀번호는 조회할 수 없다.
- 신규 계정은 최초 로그인 후 비밀번호를 변경하기 전까지 비밀번호 변경 화면 외 운영 기능에 접근할 수 없다.
- ADMIN의 비밀번호 재설정은 새 임시 비밀번호를 발급하며 기존 session도 다음 보호된 요청부터 운영 화면 접근이 차단된다. 사용자는 새 비밀번호를 변경한 뒤에만 운영 기능에 다시 접근할 수 있다.
- ADMIN은 계정 이름, 이메일, 역할과 활성 상태를 관리할 수 있지만 User를 물리 삭제할 수 없다.
- 본인 계정 비활성화, 본인의 ADMIN→TEACHER 강등, 마지막 활성 ADMIN의 비활성화와 강등을 금지한다. 항상 최소 한 명의 활성 ADMIN을 유지한다.
- User 비활성화, Teacher 비활성화, 역할 변경과 비밀번호 재설정은 변경 완료 후 다음 보호된 서버 요청부터 기존 로그인 session에도 즉시 적용한다. JWT의 과거 role/isActive 값만으로 권한을 허용하지 않는다.
- 계정 생성, 이메일 변경, 역할 변경, 활성/비활성 변경과 비밀번호 재설정은 append-only 이력으로 남긴다. 대상 User, 변경 종류, 처리 ADMIN, 처리 시각과 필요한 최소 비민감 이전/이후 값만 기록하며 평문 비밀번호, password hash, token, session 값은 기록하지 않는다.
- 기존 ADMIN의 이메일, password hash, role, isActive와 로그인 가능 상태를 migration과 seed 이후에도 보존한다. seed는 존재하는 운영 ADMIN의 password, role, isActive를 덮어쓰지 않는다.
- 권한 없는 Navigation과 UI는 숨기되 서버 권한 검증을 대체하지 않는다. 직접 URL, server query, Server Action을 모두 같은 권한 경계로 차단한다.

### Permission Matrix

| 영역 | ADMIN | TEACHER |
|---|---|---|
| 운영 Dashboard | 전체 운영·재무 | 본인 배정 클래스 운영 정보만 |
| 아이 | 전체 조회·등록·수정 | 전체 목록·상세·등록·수정 금지 |
| 배정 클래스 참가자 개인정보 | 전체 조회 | 아이 이름·보호자 연락처·안전정보 조회 |
| 안전정보·동의 | 조회·수정·관리 | 안전정보 읽기만 허용, 수정·동의 관리 금지 |
| 프로그램 | 관리 | 배정 클래스 문맥의 정보만 조회 |
| 선생님 | 전체 관리 | 본인 정보와 같은 클래스 공동 Teacher 이름만 조회 |
| 클래스 | 전체 조회·생성·반복 생성·수정·취소 | 본인 배정 클래스 조회만 |
| 예약 | 전체 조회·생성·취소 | 배정 클래스 운영 상태 조회만, 생성·취소 금지 |
| 출결 | 전체 클래스 기록 | 본인 배정 클래스 기록 |
| 결제·환불·Revenue | 조회·처리 | 금지 |
| 계정 | 전체 관리, 물리 삭제 제외 | 관리 금지 |
| 비밀번호 | 본인 변경, 계정 재설정 | 본인 변경 |

### Account Lifecycle
- ADMIN이 이메일, 이름, 역할, 활성 상태와 임시 비밀번호를 입력해 내부 계정을 만든다.
- TEACHER 계정 생성 또는 TEACHER로의 역할 변경에는 중복 연결되지 않은 활성 Teacher 한 명이 필요하다.
- 사용자는 임시 비밀번호로 인증한 뒤 비밀번호 변경 화면에서 새 비밀번호를 설정해야 운영 기능을 사용할 수 있다.
- ADMIN이 비밀번호를 재설정하면 새 임시 비밀번호가 발급되고, 해당 사용자의 기존 session은 다음 보호된 요청부터 운영 기능을 사용할 수 없다.
- ADMIN은 계정 정보를 변경하거나 비활성화할 수 있으나 본인과 마지막 활성 ADMIN 보호 규칙을 우회할 수 없다.
- 비활성 User는 로그인하거나 보호된 기능을 사용할 수 없다. TEACHER는 연결 Teacher도 활성이어야 한다.
- 계정은 물리 삭제하지 않고 비활성 상태와 append-only 변경 이력으로 운영 기록을 보존한다.

### Security Rules
- 인증과 인가는 서버가 최종 강제하며 UI 숨김만으로 권한을 허용하지 않는다.
- 보호된 요청마다 최신 DB의 User 역할·활성 상태·비밀번호 변경 필요 상태를 확인하고, TEACHER는 연결 Teacher 활성 상태와 대상 클래스 배정을 추가 확인한다.
- TEACHER의 참가자 개인정보와 ChildSafetyInfo 조회는 본인 배정 ClassSchedule 범위로 제한한다.
- 출결 Server Action은 클라이언트의 권한 식별자를 신뢰하지 않고 현재 로그인 User에서 Teacher와 ClassTeacher 배정을 서버에서 확인한다.
- 비밀번호는 hash만 저장하며 평문·hash·token·session 값을 화면, 로그와 계정 이력에 노출하지 않는다.
- 역할 변경, 비활성화와 비밀번호 재설정 후 과거 JWT 정보로 접근을 계속 허용하지 않는다.
- 계정 관리와 변경 이력에는 ADMIN 권한을 요구하며 마지막 활성 ADMIN 보호를 동시 요청에서도 우회할 수 없어야 한다.

### Schema Impact
- 현재 `User`와 `Teacher` 사이에 관계가 없으므로 TEACHER의 필수 1:1 연결과 양방향 uniqueness를 표현하는 schema 변경과 migration이 필요할 것으로 예상한다.
- 최초 로그인·재설정 후 비밀번호 변경 강제 상태를 안전하게 판정하기 위한 계정 credential 상태가 필요할 것으로 예상한다.
- append-only 최소 계정 변경 이력을 위한 계정 전용 이력 구조와 migration이 필요할 것으로 예상한다. 전체 시스템 범용 AuditLog로 확장하지 않는다.
- 기존 session의 즉시 권한 반영은 보호된 요청에서 DB를 재확인하거나 session/version 전략으로 구현할 수 있으며 정확한 구조는 PLAN에서 결정한다.
- migration은 기존 ADMIN의 role, isActive, password hash와 로그인 가능 상태를 보존해야 하며 기존 ADMIN에 Teacher 연결을 요구하지 않는다.
- 현재 `Role` enum의 ADMIN/TEACHER 값은 그대로 사용하며 새 role은 추가하지 않는다.

### Acceptance Criteria
- ADMIN은 계정 목록을 보고 내부 ADMIN 또는 TEACHER 계정을 생성할 수 있다.
- TEACHER 계정은 중복 연결되지 않은 활성 Teacher 한 명과 반드시 연결되고, 비활성 Teacher나 이미 계정이 연결된 Teacher에는 연결할 수 없다.
- ADMIN 계정은 Teacher 연결 없이 정상적으로 생성·사용할 수 있다.
- ADMIN은 계정의 이름, 이메일, 역할과 활성 상태를 변경하고 임시 비밀번호를 재설정할 수 있으며 User 물리 삭제 기능은 제공되지 않는다.
- 신규 계정과 비밀번호가 재설정된 계정은 새 비밀번호를 설정하기 전까지 비밀번호 변경 화면 외 모든 운영 화면·query·action 접근이 차단된다.
- 비밀번호는 hash만 저장되고 기존 비밀번호, 평문 임시 비밀번호, password hash, token, session 값이 조회 화면·로그·변경 이력에 노출되지 않는다.
- 본인 계정 비활성화, 본인 ADMIN→TEACHER 강등, 마지막 활성 ADMIN 비활성화·강등 요청이 서버에서 거부되고 최소 한 명의 활성 ADMIN이 유지된다.
- User 또는 연결 Teacher가 비활성화되거나 역할이 변경되면 기존 로그인 session도 변경 후 다음 보호된 서버 요청부터 최신 상태로 허용 또는 차단된다.
- 비밀번호 재설정 후 기존 session은 다음 보호된 요청부터 비밀번호 변경 화면 외 접근이 차단된다.
- TEACHER Dashboard에는 본인 배정 클래스 운영 정보만 표시되고 결제 금액, 환불, Revenue 등 재무 지표가 표시되지 않는다.
- TEACHER는 `ClassTeacher`로 본인에게 배정된 클래스만 목록과 상세에서 조회하며, 다른 클래스 직접 URL과 server query 접근은 차단된다.
- TEACHER는 본인 배정 클래스 참가자의 아이 이름, 보호자 연락처와 ChildSafetyInfo를 조회할 수 있지만 무관한 아이와 전체 아이 목록·전체 상세에는 접근할 수 없다.
- TEACHER는 ChildSafetyInfo를 수정하거나 동의 관리 화면을 조회·변경할 수 없다.
- TEACHER는 본인 Teacher 정보와 같은 배정 클래스의 공동 Teacher 이름만 조회할 수 있고 전체 Teacher 관리 화면에는 접근할 수 없다.
- TEACHER는 본인 배정 클래스의 출결만 기록할 수 있고, 서버는 현재 로그인 User→Teacher→ClassTeacher 배정을 검증한다.
- TEACHER의 클래스 생성·반복 생성·수정·취소, 예약 생성·취소, 결제·환불·Revenue, 계정 관리 요청은 UI뿐 아니라 직접 URL·query·Server Action에서도 차단된다.
- ADMIN의 기존 Dashboard, 아이·안전정보·동의·프로그램·선생님·클래스·예약·출결·결제·환불·Revenue 기능은 기존처럼 사용할 수 있다.
- 계정 생성, 이메일 변경, 역할 변경, 활성 상태 변경과 비밀번호 재설정마다 대상 User, 변경 종류, 처리 ADMIN과 처리 시각을 포함한 append-only 이력이 추가되고 기존 이력은 수정·삭제되지 않는다.
- 기존 ADMIN은 migration과 배포 후에도 기존 이메일·비밀번호·역할·활성 상태로 로그인할 수 있으며 seed 재실행이 운영 계정의 password, role, isActive를 덮어쓰지 않는다.
- ADMIN/TEACHER 권한 matrix, 직접 URL, server query, Server Action, stale session, 마지막 ADMIN 보호와 개인정보 범위를 unit 및 E2E로 검증한다.
- 모바일에서 계정 목록·계정 편집·비밀번호 변경과 TEACHER 현장 운영 화면을 가로 스크롤 없이 사용할 수 있다.

### Conflicts
- 현재 `(admin)` layout과 주요 Server Action은 `requireAdmin()`을 사용하므로 TEACHER가 사용할 화면·query·출결 action에 역할·배정 범위 인가가 필요하다. ADMIN의 기존 전체 권한은 유지한다.
- 현재 JWT session은 로그인 시점의 role을 유지하고 이후 User/Teacher 활성 상태를 재확인하지 않으므로 즉시 적용 정책을 충족하지 못한다. 보호된 서버 요청에서 최신 DB 상태를 강제하는 구조가 필요하다.
- 현재 User와 Teacher는 별도 entity이고 관계가 없어 본인 배정 클래스 판정이 불가능하다. TEACHER 필수 1:1 연결을 위한 schema/migration이 필요하지만 recurrence나 별도 권한 편집 모델은 필요하지 않다.
- 기존 Child 상세는 연락처·안전정보·동의·결제·환불을 함께 제공하므로 TEACHER에게 전체 페이지를 그대로 허용할 수 없다. 배정 클래스 운영에 필요한 최소 정보만 서버에서 제한해 제공해야 한다.
- 기존 감사 요구는 향후 범용 AuditLog 확장을 남겨 두었지만 Phase 16은 계정 변경에 한정한 append-only 최소 이력만 확정한다.
- 현재 seed의 upsert 갱신 동작은 운영 ADMIN 보존 요구와 충돌하므로 계정 관리 도입 시 기존 계정의 password, role, isActive를 덮어쓰지 않도록 정리해야 한다.

### Open Questions
없음. User–Teacher 연결, 활성 상태, TEACHER 데이터 범위와 쓰기 권한, 재무·선생님 접근, 계정·비밀번호 lifecycle, 관리자 보호, 기존 session 적용 시점과 계정 변경 이력을 사용자가 확정했다(ADR-048, ADR-049).

### Priority
Now — 여러 운영자가 최소 권한으로 같은 관리자 시스템을 사용하기 위한 인증·인가 기반이며, 선생님의 현장 출결과 안전 대응을 허용하면서 재무·전체 고객정보·관리 기능의 접근을 차단해야 한다.

---

# 23. Acceptance Criteria

### 아이
- [ ] 아이를 등록할 수 있다.
- [ ] 아이를 검색할 수 있다.
- [ ] 아이 상세에서 관계/예약/결제/환불을 볼 수 있다.
- [ ] 아이 상세에서 총 예약/참석/불참/취소/예정 5개 요약과 예정 클래스·지난 이력을 확인할 수 있다.
- [ ] 총 예약에는 CANCELLED가 포함되고 참석/불참은 출결 후 취소 여부와 무관하게 보존된 attendance로 집계된다.
- [ ] ClassSchedule이 CANCELLED이고 Reservation이 RESERVED인 미래 예약도 예정에서 제외해 지난 이력에 포함하며, 수업 취소와 예약 취소를 구분한다.
- [ ] 이력에서 예약·출결·결제 상태를 독립적으로 확인하고, 부분환불/전액환불을 구분하되 금액은 표시하지 않는다.
- [ ] 지난 이력은 최근 10건부터 표시되고 더보기 후 중복·누락 없이 이어진다.
- [ ] 아이 이력의 `historyPage`는 잘못된 값이면 1, 100 초과이면 100으로 정규화되고 누적 조회량은 최대 1000건이다.
- [ ] 취소된 ClassSchedule의 RESERVED 예약은 수업 취소·예약됨 이력을 유지하되 예약 취소 액션이 없고, 직접 취소 URL에서도 취소 form이 제공되지 않는다.
- [ ] 이력에서 기존 클래스/예약 상세 및 기존 childId prefill 예약 화면으로 이동할 수 있다.
- [ ] 모바일에서 가로 스크롤 없이 통합 이력을 확인하며, 일반 목록에 안전정보나 불필요한 개인정보가 추가되지 않는다.

### 관계
- [ ] 친구를 연결할 수 있다.
- [ ] 형제/자매를 연결할 수 있다.
- [ ] 관계를 해제할 수 있다.
- [ ] 중복 관계가 생성되지 않는다.

### 클래스
- [ ] 프로그램에서 클래스를 생성할 수 있다.
- [ ] 기존 클래스 생성 화면에서 단건/반복 등록 모드를 선택할 수 있고 기존 단건 등록 동작이 유지된다.
- [ ] 반복 등록은 같은 달 안의 시작일·종료일과 복수 요일로 생성 날짜를 정하며 양 끝 날짜를 포함한다.
- [ ] 반복 등록 전에 반복 조건, 생성 날짜 목록과 총 클래스 수를 확인할 수 있다.
- [ ] 반복 생성되는 클래스는 날짜만 다르고 프로그램·시간·장소·정원·선생님·메모·보험 정보는 동일하다.
- [ ] 반복 대상에 과거 날짜, 기존 중복 클래스 또는 validation 실패가 하나라도 있으면 어떤 클래스도 생성되지 않는다.
- [ ] 반복 등록 결과는 서로 독립적인 ClassSchedule이며 recurrence/series/group 관계를 저장하지 않는다.
- [ ] 클래스 정원의 기본값은 8명이고 관리자가 클래스별로 1~99명 범위에서 변경할 수 있다.
- [ ] 선생님 2명을 배정할 수 있다.
- [ ] 클래스를 취소할 수 있다.
- [ ] 취소 사유가 저장된다.
- [ ] 진행 예정 클래스의 RESERVED 예약만 좌석 점유로 세어 잔여 N석/만석/정원 초과 N명을 표시하고, 종료 클래스에는 좌석 정보 대신 종료를 표시한다.
- [ ] 모바일 클래스 상세에서 일반 참가자와 취소 예약을 구분하고 예약·출결·결제 상태를 확인할 수 있다.

### 예약
- [ ] 아이 1명 단위로 예약된다.
- [ ] 동일 아이 중복 예약이 불가능하다.
- [ ] 관리자는 만석 클래스에 경고를 확인한 뒤 명시적으로 초과 예약할 수 있고, 서버는 행 잠금 후 최신 RESERVED 인원과 기존 예약 검증을 다시 적용한다.
- [ ] 개인 예약을 취소할 수 있다.
- [ ] 개인 취소 사유가 저장된다.
- [ ] RESERVED 예약의 취소 가능 UI와 직접 URL은 `ClassSchedule.status=SCHEDULED AND endsAt>=now` 조건을 따르고, Server Action도 같은 조건을 독립적으로 fail-close한다.
- [ ] COMPLETED/NO_SHOW의 사후 취소와 attendance 이력 보존, 출결 없는 종료 클래스 RESERVED 취소 차단은 기존 정책대로 유지된다.
- [ ] 클래스 상세와 아이 상세 양쪽에서 동일한 예약 생성 흐름으로 진입할 수 있다.
- [ ] 출결 후 취소된 예약은 취소 상태와 보존된 참석/불참 이력을 함께 표시한다.
- [ ] 참가자 통합 화면은 PAID/PARTIAL_REFUNDED/REFUNDED를 결제완료로, CANCELLED/Payment 없음을 미결제로 요약하되 출결이나 환불 정책을 변경하지 않는다.

### 환불
- [ ] 아이별 환불이 가능하다.
- [ ] 환불금액을 기록할 수 있다.
- [ ] 환불 사유를 기록할 수 있다.
- [ ] 결제금액보다 많은 금액을 환불할 수 없다. (부분 환불을 여러 번 해도 합계가 초과할 수 없다)

### 매출
- [ ] 기간별 매출을 조회할 수 있다.
- [ ] 프로그램별 매출을 조회할 수 있다.
- [ ] 환불이 순매출에 반영된다.

### 대시보드
- [ ] 로그인 후 첫 화면에서 월간 캘린더로 날짜별 클래스 수와 예약 인원을 확인할 수 있다.
- [ ] 캘린더에서 취소된 클래스는 제외되고 예정/완료 클래스는 포함된다.
- [ ] 날짜를 선택하면 해당 날짜의 예약자 명단을 화면 안에서 확인할 수 있다.
- [ ] 예약자 명단은 클래스별로 그룹화되고, 출결 없는 사전 취소 예약은 제외되며 출결 후 취소 예약은 보존된 출결 이력과 함께 표시된다.
- [ ] 예약자 명단에서 아이 상세 화면으로 이동할 수 있다.
- [ ] 오늘 예약 현황과 오늘 취소 처리 건수가 15.6의 KST 및 상태 기준과 일치한다.
- [ ] 오늘 순매출이 ADR-040의 오늘 결제액-오늘 완료 환불액 기준과 일치한다.
- [ ] 대시보드가 특정 예약을 환불 처리 필요 건으로 자동 판정하지 않는다.

### 계정 및 권한
- [ ] ADMIN은 내부 ADMIN/TEACHER 계정을 생성하고 이름·이메일·역할·활성 상태와 임시 비밀번호를 관리할 수 있다.
- [ ] TEACHER User는 기존 활성 Teacher와 필수 1:1로 연결되고 중복 연결은 허용되지 않는다.
- [ ] 신규·비밀번호 재설정 계정은 비밀번호 변경 전 운영 기능 접근이 차단되며 비밀번호는 hash만 저장된다.
- [ ] 본인 계정 비활성화, 본인 ADMIN 강등, 마지막 활성 ADMIN 비활성화·강등이 서버에서 거부된다.
- [ ] 계정·Teacher 비활성화, 역할 변경과 비밀번호 재설정은 기존 session에도 다음 보호된 서버 요청부터 적용된다.
- [ ] TEACHER는 본인 배정 클래스 운영 정보와 해당 참가자의 아이 이름·보호자 연락처·안전정보만 조회할 수 있다.
- [ ] TEACHER는 본인 배정 클래스의 출결만 기록할 수 있고 그 외 클래스·예약·재무·계정 쓰기는 차단된다.
- [ ] TEACHER에게 결제·환불·Revenue·재무 Dashboard·전체 아이·전체 Teacher 관리가 노출되지 않는다.
- [ ] Navigation 숨김과 별개로 직접 URL, server query와 Server Action에서 최신 DB 역할·활성·배정 범위를 검증한다.
- [ ] 계정 생성과 주요 변경이 append-only 이력으로 남고 비밀번호·hash·token·session 값은 기록되지 않는다.
- [ ] 기존 ADMIN의 로그인·역할·활성 상태가 migration과 seed 이후에도 유지된다.

### 보안
- [ ] 인증되지 않은 사용자가 관리자 데이터를 조회할 수 없다.
- [ ] 권한 검증이 서버에서 수행된다.
- [ ] 개인정보가 로그에 노출되지 않는다.

### 동의
- [ ] 동의와 철회가 이력으로 남는다.
- [ ] 철회 후 재동의해도 이전 이력이 보존된다.
- [ ] 사진 홍보 활용에 동의하지 않은 아이를 외부 공개 대상에서 제외할 수 있다.

## Definition of Done
목록 화면의 상태 필터 동작과 상태에 따른 버튼/링크 노출(예: 완료로 표시되는 클래스의 예약 추가/정보 수정/클래스 취소 버튼 숨김, 완료로 표시되는 예약의 취소 버튼 숨김, 재예약 차단 등)은 Playwright E2E 테스트로 검증하고 로컬에서 통과를 확인한 뒤에만 완료로 본다(docs/DECISIONS.md ADR-028 참고). 이 규칙은 Phase 5에 한정되지 않고, 앞으로도 같은 성격의 작업(목록 필터, 상태 기반 UI 노출)에 일반적으로 적용된다.

---

# 24. 아직 결정하지 않은 사항

다음은 개발 중 임의로 결정하지 말고 Issue 또는 Architect 단계에서 확정한다.

- 보호자 정보를 별도 Entity로 분리할지 여부
- 온라인 결제 연동 여부
- 결제수단별 실제 처리 방식
- 문자/카카오 알림
- 날씨 자동 연동
- 예약자용 고객 화면 제공 여부
- 쿠폰/프로모션 등 별도 할인 정책 엔진 도입 여부 (Phase 8은 관리자 할인금액 자유 입력만 제공하며 정책 엔진은 범위 밖 — ADR-038)

## 예약 관련
- 예약 취소/재예약 시 이전 취소 이력(취소일시/사유/처리자)이 현재 행에 덮어써져 사라지는 것을 그대로 둘지, 별도 append-only 이력 테이블(ChildConsent 패턴 참고)을 도입해 보존할지. ADR-024 참고. 취소·재예약 빈도가 실제로 확인되면 판단한다.
- ClassSchedule.status를 실제로 DB상 COMPLETED로 전환하는 메커니즘(배치/크론, 또는 관리자가 누르는 수동 확정 버튼 등)을 도입할지 여부는 여전히 미결정이다. 지금도 클래스 자체의 "완료" 판정은 조회 시점 계산으로 화면(목록 필터 포함)에만 표시/조회하고 DB는 건드리지 않는다(ADR-026, ADR-028). 이 프로젝트에는 현재 배치/크론 인프라가 없다(`.github/workflows/`에 CI만 존재)는 점도 함께 고려해야 한다. (Reservation.status의 COMPLETED/NO_SHOW 전환은 Phase 6 출결 기록을 통해 해소되었다 — ADR-031 참고. 이 항목은 ClassSchedule.status에 대해서만 남는다.)
- 이미 종료된 클래스에 대한 사후 취소가 필요한 경우(예: 우천으로 취소해야 했는데 당일 처리를 못 하고 클래스 시간이 지나버린 경우)를 어떻게 처리할지. 지금은 종료된(표시상 ENDED) 클래스의 취소/수정과 그 소속 예약의 취소를 서버가 차단한다(ADR-027). 실제로 이런 사례가 발생하면 그때 필요한 방법(예: 시간 제한 없는 사후 취소 허용, 별도 관리자 전용 강제 취소 경로 등)을 정한다.
- 클래스 정원 동시성 보장(ADR-023, `SELECT ... FOR UPDATE` 기반 직렬화)이 실제 병렬 요청을 재현하는 자동화 테스트로 커버되지 않는다. 현재 `tests/unit/reservations/actions.test.ts`의 정원 관련 테스트는 모두 `$transaction`을 mock으로 즉시 실행하는 단일 스레드 시나리오만 검증하며, 여러 `createReservationCore` 호출을 실제 DB에 동시에 실행해(`Promise.all` 등) 정원 초과(overselling)가 발생하지 않는지 CI에서 반복 검증할 수 있는 통합 테스트는 없다. 배포 전 수동/스크립트 기반 검증에만 의존하고 있다는 한계를 ADR-023 Consequences도 스스로 인정하고 있다(Phase 5 코드 리뷰, 2026-08-23). 실제 동시 예약 상황에서 문제가 확인되거나 이런 통합 테스트를 CI에 자동화할 필요가 확인되면 그때 도입한다.

## 결제/환불 관련 (Phase 8)
2026-08-26 실사용 테스트 중 추가된 5개 Open Question과 Phase 8 설계 중 발견된 2개 blocker는 모두 사용자가 직접 결정해 해소되었다(docs/DECISIONS.md ADR-033~039, 22장 Phase 8 참고). Phase 8 Open Questions는 없다.
- ~~이미 출결이 기록되어 COMPLETED/NO_SHOW로 전환된 예약의 취소를 허용할지 여부~~ → 허용한다(ADR-033).
- ~~허용될 경우 Reservation.status를 어떤 값으로 되돌릴지~~ → CANCELLED로 직접 전환한다. attendance 등 출결 이력 필드는 지우지 않고 보존한다(ADR-033).
- ~~노쇼(NO_SHOW) 예외 환불을 허용하는 조건~~ → 관리자 재량 + 사유 필수 기록으로 허용하며, 사유 화이트리스트나 승인자 지정 절차는 두지 않는다(ADR-034).
- ~~출결 기록 후 예약 취소 시 환불이 자동으로 트리거되는지~~ → 자동 트리거하지 않는다. 예약 취소와 환불은 별개 동작이며, 환불은 관리자가 결제/환불 화면에서 별도로 수동 처리한다(ADR-035).
- ~~부분 환불의 금액 계산 방식~~ → 관리자 자유 입력으로 한다(비례 자동 계산 아님). 환불액이 결제액을 초과할 수 없다는 기존 상한 제약(13.4, ADR-007)은 그대로 유지된다(ADR-036).
- ~~환불 승인 절차(결재선 존재 여부)~~ → 모든 환불은 관리자 1인이 별도 승인 단계나 결재선 없이 처리한다. 현재 1인 운영 규모에서는 승인 단계가 과설계이므로 두지 않으며, 직원 증가나 운영 규모 확대 시 재검토한다. 영구적인 정책은 아니다(ADR-037).
- ~~할인금액 입력 정책~~ → 관리자가 0 이상 정가 이하의 할인금액을 자유 입력하고 `paidAmount = amount - discountAmount`로 계산한다. 별도 할인 정책 엔진은 Phase 8 범위에 포함하지 않는다(ADR-038).
- ~~일반 환불 자격~~ → 결제가 존재하고 남은 환불 가능 금액이 있으면 RESERVED / COMPLETED / NO_SHOW / CANCELLED 및 클래스 전체 취소 건을 모두 같은 관리자 수동 환불 흐름으로 처리한다. 자동 환불은 하지 않고, NO_SHOW만 상세 사유가 필수다(ADR-039).

Open Questions: 없음.

## 안전 정보 관련
- 민감정보 보관 기간 및 파기 시점
- 동의서를 종이로 받을지 앱에서 받을지
- 보험을 클래스 단위로 가입하는지 연간 단위인지 (실제 계약 형태는 여전히 미확인. 다만 Phase 6에서 클래스 생성/수정 화면에 "직전 클래스 값 불러오기" 편의 기능을 추가해, 계약 형태와 무관하게 반복 입력 부담은 낮췄다 — ADR-032 참고.)

## 기관 단체 관련 (확대 계획 있음, 도입 시점 미정)
- 기관 수업의 정원 기준(8명 유지 여부)
- 기관 소속 아이를 개별 등록할지, 인원수만 관리할지
- 기관 일괄 결제 시 세금계산서/청구 처리
- 기관별 매출 집계 필요 여부
- 기관 담당자 연락 체계

## 인증/보안 관련
- 로그인 실패 응답 시간 차이로 계정 존재 여부가 노출되는 타이밍 사이드채널을 막을지 (더미 해시 비교 도입 여부)
- 인가 검증을 middleware.ts로 일원화할지, 현재처럼 라우트 그룹별 layout에서 개별적으로 수행할지 (Phase 2 이후 보호 라우트가 늘어날 때 재검토)
- DB CHECK 제약(`child_name_not_blank`)이 `btrim` 기반이라 탭/개행 문자로만 구성된 이름을 걸러내지 못하는 한계 (서버 Zod가 1차 방어라 현재는 위험 낮음, DB 제약을 강화할지 여부)
- 아이 목록 검색어(`q`)가 GET 쿼리스트링으로 URL에 그대로 노출되어 브라우저 히스토리/서버 액세스 로그에 남을 수 있는 문제 (POST 기반 검색으로 바꿀지, 그대로 둘지)
- 아이 검색 폼(`child-search-form.tsx`)이 `type="date"`/`type="search"` 의 브라우저 기본 HTML5 검증에만 의존하는 것을 유지할지, 서버/클라이언트 검증을 추가할지

## 아이 관리 관련
- `Child.memo` (운영 메모) 에 길이 제한을 둘지 여부, 둔다면 상한값

## 테스트 인프라 관련
- 상태를 가진 UI 컴포넌트(NavDrawer, Disclosure 등)를 자동으로 검증할 렌더/상호작용 테스트 인프라(jsdom, @testing-library/react 등) 도입 여부. 현재는 순수 함수 단위테스트만 존재하고 컴포넌트 자체는 코드 리뷰로만 검증된다. 상태 기반 UI가 계속 늘어날수록 이 공백이 커진다. Playwright e2e를 CI에 편입할지를 다루는 ADR-009 와 함께 검토할 사안(둘 다 "테스트를 어디까지, 어떤 방식으로 자동화할 것인가" 라는 같은 질문에 속한다).

## 클래스 화면 관련
- 클래스 목록의 캘린더 요구는 2026-08-24 실사용 테스트를 통해 Phase 10의 캘린더형 홈 화면 요구로 구체화되었다. 홈 캘린더의 확정 범위와 규칙은 15.4~15.7 및 ADR-042를 따른다. 기존 클래스 목록 뷰를 병행/대체할지는 Phase 10 범위가 아니라 별도 클래스 화면 결정으로 남긴다.

## 코드 정리 관련
- `read*FormValues` 헬퍼(검증/서버 재확인 실패 시 사용자가 입력한 원본 값을 폼에 돌려주는 함수)가 Child/Teacher/Program/Class 4개 도메인의 `actions.ts` 에 거의 동일한 형태로 중복 구현되어 있음. 공용 유틸(예: `lib/shared/`)로 추출할지, 도메인별 필드 차이가 커서 그대로 둘지 (Phase 4 코드 리뷰에서 발견).
- `components/ui/checkbox.tsx` 가 `<input type="checkbox" {...props} />` 순서로 구현되어 있어, 호출부가 실수로 `type` prop 을 넘기면 `{...props}` 가 뒤에서 덮어쓸 수 있음. `type` 을 destructure 해서 고정할지 여부 (현재 실사용 코드는 이 문제를 겪지 않는 잠재적 위험, Phase 4 코드 리뷰에서 발견).
